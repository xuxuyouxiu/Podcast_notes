# 更新日志

所有值得注意的项目变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，\
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.52.13] - 2026-08-30

### 修复
- **OSS 同步脚本自检误判导致 Release 假失败**：v1.52.12 实际已把改写后的 yml 镜像到全部历史目录（安装包 200 可下载，更新链路已恢复），但 `yaml.safe_dump(sort_keys=True)` 把 `version:` 字段排到了文档末尾，脚本自检只读头 256 字节找版本号 → 误报失败。现保持原字段顺序（`sort_keys=False`）且全文匹配版本号。

## [1.52.12] - 2026-08-30

### 改进
- **更新通道同步提速约百倍**：v1.52.11 的镜像方案要往每个历史目录复制 115MB 安装包，海外 runner 跨洋上传需约 1 小时（该次发布已取消）。改为「yml 绝对 URL 镜像」：latest.yml 内的下载地址改写为指向新版本目录的绝对 URL（electron-updater 的 `new URL(url, base)` 语义原生支持，已验证），各历史目录仅同步 2KB 的 yml——任意旧版本客户端依然能在自己目录内发现新版本并直达新目录下载，同步从小时级降到秒级。

## [1.52.11] - 2026-08-30

### 修复
- **应用内检查更新恒报「已是最新」**：OSS 更新通道探测的是「当前运行版本自己的目录」（`download/v{运行版本}/latest.yml`），而各目录里永远只放着那个版本自己的 yml——1.52.9 客户端读到 1.52.9，结构上永远检测不到更新。修复：发布时把新版 latest.yml + 安装包 + blockmap 镜像写入所有已存在的 `download/v*/` 目录（scripts/sync-release-oss.py），任意旧版本客户端在自己目录内即可完成检测与下载，无需先升级即可被解救。

## [1.52.10] - 2026-08-30

### 新增
- **笔记标签栏右键菜单（Obsidian 式批量关闭）**：在标签上右键即可选择 关闭 / 关闭其他标签 / 关闭左侧标签 / 关闭右侧标签 / 关闭全部标签，不必再逐个点 X。激活标签被批量关闭时自动落到邻近标签；点击外部或 Esc 关闭菜单。

## [1.52.9] - 2026-08-30

### 改进
- **知识关联 / 搜索面板响应式排版**：两面板字号此前固定 10–13px，窗口最大化后依旧偏小。现改为随窗口宽度平滑放大（中屏 ≈ 原大小仅微调最小两档，1920px 最大化约 +25%，2560px 封顶），仅作用于这两个面板。配套布局伸缩：左侧实体列表 / 筛选侧栏宽度随字号增长，超宽窗口下限制结果行宽保证可读性，搜索摘要改用 3 行 line-clamp 防止字号变大后截断错位。

## [1.52.8] - 2026-08-30

### 修复
- **笔记内外部链接改为跳转系统默认浏览器**：此前在笔记库点击概念/术语等笔记中的外部网页链接，会在应用主窗口内直接整页打开该网页——无边框窗口被顶掉后没有关闭/返回按钮且卡顿。现渲染层拦截点击转交默认浏览器打开；主进程另加 `will-navigate` 兜底，任何漏网的主框架导航一律拦截（http/https 转交浏览器，其余阻止），外部网页永远不会再顶掉应用界面。

## [1.52.7] - 2026-08-24

### 改进
- **发布产物同步 OSS 时额外上传固定路径 `download/latest.yml`**（不带版本号、禁缓存）：官网/外部探测最新版本号不再需要先知道版本号才能拼出带版本的路径（鸡生蛋问题），为官网下载区自动展示最新版本铺路。

### 清理
- 删除分享卡遗留的 `extractChips` 函数（v1.46.0 知识卡片重做后模板已不使用），lint 警告清零。
- 移除 `podcast.ts` 中失效的 eslint-disable 注释（fetchOgTitle 实际有使用）。
## [1.52.6] - 2026-08-22

### 修复
- **OSS 更新通道默认激活**：v1.52.5 的 OSS 地址误做成读运行时环境变量，打包应用无人设置导致通道永不生效。现默认硬编码 bucket 直连地址（podmuse.oss-cn-wuhan-lr.aliyuncs.com，允许环境变量覆盖）。用户 Secrets 已配置，本版发布起安装包将自动同步 OSS 并作为首选更新通道。

### 改进
- OSS 更新通道切换到 CDN 加速域名 `dl.xuxuya66.top`（阿里云 CDN，源站 bucket podmuse）：国内边缘节点分发，下载更快；回源链路已实测打通。

## [1.52.5] - 2026-08-22

### 新增
- **自有阿里云 OSS 更新通道（三通道之首）**：发布时 release workflow 自动把安装包/latest.yml 同步到 OSS bucket `podmuse`（武汉本地地域），应用检查更新优先走 OSS——国内全速、不依赖 VPN 与第三方镜像；OSS 未配置或不可达时依次回落 GitHub 直连 → 公共镜像。
- 发布流程新增 `scripts/sync-release-oss.py`：上传产物到 `download/v{version}/`，安装包长缓存、latest.yml 禁缓存，并校验版本号防传错。

### 说明
- 需在仓库 Secrets 配置 `OSS_AK_ID` / `OSS_AK_SECRET` / `OSS_ENDPOINT` 后，OSS 通道才生效；未配置时行为与 v1.52.4 一致（双通道），不会失败。

## [1.52.4] - 2026-08-22

### 修复
- **无 VPN 时应用内「检查更新」查不到更新**：更新源为 GitHub Releases（api.github.com / github.com），国内直连常被重置，且检查失败静默降级表现为「没有更新」。现在检查前先探测 GitHub 可达性（4s 快速失败）：可达走原 GitHub 通道；不可达自动切换国内镜像源（ghfast.top → gh-proxy.com → mirror.ghproxy.com）读取版本信息与下载安装包，全部不可用才放弃。实测镜像可正常取到 latest.yml 与安装包。

### 改进
- 手动点「检查更新」同样享受双通道，无 VPN 环境也能检测并下载新版本。

## [1.52.3] - 2026-08-22

### 修复
- **抖音分享短链（v.douyin.com/xxx）报「不支持的类型： short」根治**：短链需先跟随 302 解析为完整链接，但下载器的裸客户端解析常被风控拦截且失败时静默跳过，最终抛出用户无法理解的「不支持的类型： short」。现在主进程在调用下载器前自行解析短链（带浏览器 UA + 登录 cookie，响应 HTML 兜底提取），解析成功直接用完整链接下载；仍失败时给出可行动提示（请复制浏览器地址栏完整链接重试）。

## [1.52.2] - 2026-08-22

### 修复
- **抖音下载全部失败（报「抖音下载失败 (exit 1)」）根治**：应用同步抖音登录态到下载器 config.yml 时，cookie 值不加引号裸写，本次会话 cookie 含 `%` 开头的值（如 home_can_add_dy_2_desktop=%220%22 等 6 处），导致 YAML 解析崩溃、下载脚本启动即挂。现在写入前做 YAML 安全转义（特殊字符加引号并转义），并已修复本机损坏的 config.yml（实测下载恢复 success:true）。同场景回归测试覆盖。
- **下载失败错误信息可定位**：脚本崩溃无 JSON 输出时，错误信息附带 stderr 最后几行摘要，不再只显示 exit 1。

## [1.52.1] - 2026-08-22

### 修复
- **阿里云百炼适配器按真实联调重写**（用用户真实 API Key 端到端验证）：上传凭证接口实为 `GET /api/v1/uploads?action=getPolicy&model=...`；文件经 OSS PostObject 表单上传后，提交任务须用 `oss://{objectKey}` 引用格式且**必须携带 `X-DashScope-OssResourceResolve: enable` 头**，否则服务端读不到文件（此前实现按公开文档的 file_urls/https 公网 URL 写法会以 FILE_DOWNLOAD_FAILED / SERVER_ERROR 失败）。
- **转写模型升级**：`paraformer-v2` → `qwen-audio-3.0-asr-flash-filetrans`（官方当前非实时首推：热词、Prompt 上下文、说话人分离、多语种自动识别，单文件 ≤12 小时）。同日实测对比：新模型转写全对，旧 paraformer 漏字。该模型多语种自动识别，「语音识别语言」设置对其不生效（讯飞/本地仍生效）。
- 设置页阿里引擎文案改为「按量计费」（原标注价格为旧 Paraformer 价格，与新模型不符）。

## [1.52.0] - 2026-08-22

### 新增
- **云端语音转写引擎（阿里云百炼 / 讯飞）**：低配电脑无需本地 Whisper 模型也能转写。设置 → 转写偏好新增「转写引擎」三选一：本地 Whisper（免费，默认不变）/ 阿里云百炼 Paraformer（约 0.29 元/小时，每月免费 10 小时）/ 讯飞语音转写（效果优先，新用户最多免费 50 小时）。选云端后填入对应 API Key 即用，中文效果第一梯队。
- **云端失败自动降级本地**：云端转写失败（网络/鉴权/余额/超时）时自动切回本地 Whisper 重试并 toast 提示原因；云端凭据未配置时同样自动用本地，任务永不中断。
- **凭据安全**：阿里/讯飞 API Key 走 safeStorage 加密落盘（enc:v1），输入框密码态，日志与提示永不出现明文。

### 改进
- **转写架构可扩展**：语音转写收敛为统一引擎抽象层（src/main/transcriber/），本地/云端均为可插拔适配器，后续接入新厂商只需新增适配器文件。

## [1.51.17] - 2026-08-21

### 改进
- **飞书凭据区重新布局**：彻底移除「高级模式」折叠，App ID / App Secret 必填字段改为卡片平铺展示（含说明、自动获取群列表、测试连接），打开设置即可看到；剪贴板复制 App ID（cli_）或 Chat ID（oc_）自动识别填入。
## [1.51.16] - 2026-08-21

### 修复
- **飞书 OAuth「飞书接口返回异常」根治**：授权 URL 此前未携带 scope，换取到的 user_access_token 没有 `im:chat:readonly` 群列表权限，连接成功但拉群失败。现在授权页会明确请求「获取群信息」权限（授权时请勾选）；失败时错误信息附带飞书返回的 msg（如 no permission），可直接定位原因。

### 说明
- 若授权页未出现群权限勾选项：请先在飞书开发者后台为该应用开通 `im:chat:readonly` 权限并创建版本发布，再重新授权。
## [1.51.15] - 2026-08-21

### 新增
- **飞书「自动获取群列表」：Chat ID 免复制**：三字段直连模式填好 App ID / App Secret 后，点「自动获取群列表」即可列出机器人所在群聊（im/v1/chats），下拉直接选择；换群随时下拉切换，无需再进群设置复制 oc_ 开头的 Chat ID。

### 说明
- 使用前提（一次性，飞书平台约束）：应用已开通 `im:chat:readonly` 权限并发布，机器人被拉进目标群；App ID / App Secret 仍需在开发者后台创建并复制（飞书不允许 API 读取密钥）。
## [1.51.14] - 2026-08-21

### 修复
- **飞书授权页 20029「重定向 URL 有误」根治**：OAuth 回调地址由随机端口改为固定地址 `http://localhost:47839/feishu/callback`，设置页直接展示并可一键复制；首次使用时把它添加到飞书开发者后台 → 应用 → 开发配置 → 安全设置 → 重定向 URL（仅需一次），授权即可通过。
- **Notion 授权回调对称修复**：本地回调固定为 `http://localhost:47840/notion/callback` 并在设置页展示引导（Public integration → Redirect URI 登记一次）；端口被占用时给出明确提示。

### 改进
- **飞书凭据不再折叠隐藏**：App ID / App Secret 为必填，高级模式折叠区默认展开（Chat ID 可留空），打开设置即可看到，减少「找不到连接飞书入口」的困惑。
## [1.51.13] - 2026-08-21

### 修复
- **Notion「连接功能准备中」不再卡死**（与飞书对称）：导出设置新增「OAuth 连接服务配置（可选，Public Integration）」折叠区，填写 Notion Public integration 的 Client ID / Client Secret 并保存后，自动同步进 OAuth 凭据（notion_oauth），「连接 Notion」按钮立即可用，可走浏览器授权流程（免手动复制 Token）；已有 OAuth 连接不受影响。

### 改进
- **连接入口更清晰**：飞书/Notion 连接按钮未配置时直接显示「连接飞书（未配置）」/「连接 Notion（未配置）」，并附引导文案（飞书：填 App ID/App Secret 保存即可启用，Chat ID 可留空；Notion：粘贴 Token 或填 OAuth Client ID/Secret 保存即可启用），避免用户找不到入口、误以为必须填三个字段。
## [1.51.12] - 2026-08-21

### 修复
- **飞书「连接服务准备中」不再卡死**：此前 OAuth 连接服务的凭据（feishu_oauth.appId/appSecret）没有写入入口，设置页「高级模式（自建应用）」填写的 App ID / App Secret 不会同步过去，按钮永远置灰。现在保存配置时自动同步：填一次凭据保存后，按钮立即变为「连接飞书」，可走浏览器扫码授权（免手动复制 Chat ID）；已有 OAuth 连接（token/群聊）不受影响。
## [1.51.11] - 2026-08-21

### 新增
- **图文指南补齐真实截图**：ai-key（3 张）、feishu（4 张）、douyin（2 张）、notion（1 张）共 10 张截图上线，覆盖申请 API Key、飞书建应用/机器人/群聊、抖音扫码登录、Notion 新建连接；截图均已做敏感信息打码（API Key、分享链接 token）。

### 改进
- **Notion 指南只需 1 张截图**：新建连接步骤配图，「复制令牌」「数据库授权」两步为纯文字说明（占位卡展示），与「只需一个令牌即可用」的实际流程一致。
## [1.51.10] - 2026-08-21

### 修复
- **Notion 令牌不再因保存/更新丢失**：config:save 增加兜底保护——无论表单传回何种形态，export.notion.token 为空时一律还原主进程已保存值（覆盖早期版本「更新后已保存令牌丢失」问题），并补充异常形态回归测试。
- **流程面板动画恢复**：移除「系统减少动效时关闭全部动画」的覆盖（v1.51.8 引入，导致处理中进度流光/加载图标不再运行），各动画恢复自然速度。

### 改进
- **设置排版整理**：设置弹窗在全屏/最大化窗口下自动放大（940×660 / 1080×720），内容区更宽松；「工具维护」改为纵向卡片布局，清理临时文件/软件更新/浏览器剪藏三块整齐一致。
- **图文指南精简**：「安装语音识别引擎」「选择笔记目录」不再需要截图，改为纯文字说明并标注入口位置（语音模型/转写偏好设置页），用户按文字即可自行完成设置。
## [1.51.9] - 2026-08-21

### 修复
- **yt-dlp 失败提示不再只报「退出码 1」**：提取音频/获取元数据失败时，错误信息附带 yt-dlp 最后输出的原因（如 ffmpeg 未安装、YouTube 反爬提示等），遇到问题可直接定位，不用再猜。

## [1.51.8] - 2026-08-21

### 修复
- **加载图标旋转过快**：`prefers-reduced-motion: reduce` 下不再用 `0.01ms` 加速旋转动画，改为直接关闭动画/过渡。
- **yt-dlp 内置版本升级到 2026.08.19**：修复 YouTube 新版页面解析/反爬兼容问题，提升 `--extract-audio --audio-format mp3` 成功率。

## [1.51.7] - 2026-08-21

### 修复
- **yt-dlp 转 MP3 失败（退出码 1）根治**：PodMuse 调用 `--extract-audio --audio-format mp3` 时依赖命令行 ffmpeg/ffprobe，但此前只打包了 Electron 运行库 `ffmpeg.dll`，并非 yt-dlp 所需的 CLI 工具。新增 `src/main/platforms/ffmpeg.ts`：自动检测已安装/已打包的 ffmpeg+ffprobe，缺失时自动下载到用户数据目录并在 yt-dlp 调用中传入 `--ffmpeg-location`，全程无需用户手动安装。

## [1.51.6] - 2026-08-20

### 新增
- Notion 设置页高级模式新增「**刷新数据库列表**」下拉：一键列出当前 Token 可访问的数据库并选择填入（免手动找 database id）；401/403、网络错误均有可读提示

## [1.51.5] - 2026-08-20

### 修复
- Notion database ID 识别支持新版 36 位 UUID（带连字符，Notion 新版页面 URL 格式）与旧版 32 位 hex 两种

## [1.51.4] - 2026-08-20

### 改进
- Notion 图文指南第 3 步明确「数据库页面在工作区左侧边栏（表格/看板页），不在开发者工具里」，并给出没有数据库时用 /table 新建的方法

## [1.51.3] - 2026-08-20

### 修复
- **Notion 平台改版适配**：Notion 已用「连接（Connections）」取代旧「我的集成」，新令牌以 `ntn_` 开头（旧版 `secret_`）——图文指南（manifest 中英文）、设置页 Token 输入框提示、剪贴板自动识别全部支持新格式（`ntn_` / `secret_` 双前缀），截图素材清单同步更新
- 指南第 3 步明确：必须把连接授权到目标数据库页面（… → 连接 → 连接到），数据库才会出现在列表

## [1.51.2] - 2026-08-20

### 修复
- **登录后不再显示「待验证」**：登录标记（sessionid 等）出现即视为真实登录成功，直接保存「已连接」——昵称改为尽力而为（页内上下文探测带重试 6×1s，回退主进程校验），都拿不到也显示「✓ 已连接抖音」而非「待验证」；老用户（有 cookie 无状态）同样直接显示已连接
- 页内账号信息探测加重试：登录跳转后信息可能稍后才就绪，每 1s 重试最多 6 次再关窗

## [1.51.1] - 2026-08-20

### 修复
- **抖音「每次打开都要重新登录」**：启动/例行校验不再判死登录态——校验失效只保留既有 connected 状态，不再标「已失效」；只有真实下载/使用失败时才标过期（新增 `markDouyinExpired`，由下载链路调用）
- **登录后显示「已连接 · 昵称」**：登录成功后在登录页上下文内拉取账号信息（同源请求带全量 cookie/反爬头，最可靠），拿不到昵称才回退主进程校验——修复此前一直停留在「待验证，将自动重试」、拿不到昵称的问题
- **下载 cookie 自愈**：下载抖音视频时优先取内嵌会话的新鲜 cookie（会话优先，回退配置冻结串），并自动写回配置与 douyin-downloader 的 config.yml（下载器实际读取的配置）
- **bytedance:// 协议弹窗彻底根治**：新增全局 `web-contents-created` 导航防护（will-navigate + will-frame-navigate），主窗口/登录窗/子弹窗/iframe 的**所有**非 http(s) 协议导航一律拦截（此前只覆盖 window.open 与主框架导航）

### 测试
- douyin-auth 新增/更新 8 例：页内昵称、非破坏性刷新、markExpired、会话取鲜、config.yml cookies 块同步（保留其它配置）
- 全量 466 测试通过

## [1.51.0] - 2026-08-20

### 改进
- **统一类目色 / 实体色设计令牌**：在 `globals.css` 新增 `--cat-tech/news/psych/life`（4 类目色）与 `--entity-people/projects/concepts/terms`（4 实体色）token，浅/深色主题全覆盖；BacklinkPanel、SearchPanel、NoteMarkdown、QAPanel 不再各自维护色表——抽取共享 `src/renderer/lib/category-colors.ts`，同一「科技商业」在关系图谱页与搜索页颜色一致，消除品牌色硬伤
- **补齐缺失令牌并修复失效渲染**：定义 `--text-tertiary` / `--text-tertiary-rgb`（5 处引用此前静默 fallback 成灰）、`--bg-hover` / `--border-subtle` / `--accent-light`（SearchPanel/ExportMenu 此前 hover 态透明）、`--mark-bg`（关键词高亮走主题）；清理 `var(--warning, #eab308)`、`var(--bg)`、`var(--bg-primary)`、`var(--radius)`、`var(--danger)` 等失效引用与硬编码兜底
- **字号系统收敛**：将 `--fs-*` token 扩展为 10/11/12/13/14/15/18/20/24/64px 规整刻度；`globals.css` 与迁出的组件 CSS 中 262 处裸写 `font-size: XXpx` 全部改走 token，消除 9.5/10.5/11.5/12.5/13.5/16.5 等 0.5 步进值与孤儿字号
- **设置页行内样式收敛为 class**：新增 `st-` 前缀工具类与 `settings-dialog__*` BEM 布局类，TabWhisper / TabPlatforms / SettingsDialog 三处共 70+ 处 inline style 改走 class，设置页导航、状态徽标、进度条、硬件提示统一走设计系统
- **统计卡去多色**：历史页 5 张统计卡 icon 由紫/绿/红/蓝/橙 5 色改为「accent 主色 + 成功/失败语义色」，KPI 视觉不再为差异化而差异化
- **关系图谱画布渐变在浅色主题下可见**：画布背景改走 `--graph-canvas-bg` / `--graph-canvas-shadow`，浅色主题下不再与底色融为一体
- **知识图谱 dim 强度调整**：节点悬停时非连接节点 dim 从 0.15 提到 0.4、过渡缩到 0.18s，不再像「图谱坏掉」
- **无障碍降级**：补充 `@media (prefers-reduced-motion: reduce)` 全局动画/过渡降级

### 重构
- **内联 `<style>` 块迁出**：BacklinkPanel（700+ 行）、SearchPanel（70+ 行）的内联 CSS 迁出到 `src/renderer/styles/backlink-panel.css` / `search-panel.css`，App.tsx 的 `@keyframes toastIn` 迁入 globals.css；SettingsDialog 重复的 fadeIn/modalSlide keyframes 删除、settings-checkbox/link-button 并入全局样式——便于 Vite 提取、缓存与 HMR，避免 React 严格模式 hydration mismatch
- **代码依赖分析**：用 CodeGraph 工具生成项目结构/调用依赖概览，作为本次优化的结构基线

### 修复
- **TabWhisper 下载进度条底色**：进度条 `background: var(--bg)` 引用了未定义 token 导致底色透明，改走 `--bg-surface`

### 测试
- 全量 462 测试通过；typecheck 0 错误；lint 0 错误（仅 2 条既存无关 warning）

## [1.50.5] - 2026-08-20

### 修复
- **登录前反复弹出谷歌商店**：登录窗弹窗规则新增应用商店/下载引导域名黑名单（play.google.com / apps.apple.com / app.adjust.com）静默拦截；外部链接增加 3 秒最小间隔限流，页面反复 window.open 不再反复拉起浏览器；登录窗主框架禁止导航离开抖音域（防广告/商店页劫持登录流程）
- **登录成功但窗口不关闭、配置不保存**：「关窗」与「接口校验」解耦——登录标记（sessionid 等，仅真实登录后出现）命中即关窗并保存（先标 unverified，随后自动校验升级为已连接+昵称）；轮询改为全量抓取 cookie + 域名后缀过滤，避免子域 cookie（sso.douyin.com 等）漏检；校验接口的任何响应形状问题都不再阻塞登录完成，失效态由后台自动重验自愈

### 测试
- douyin-auth 新增/更新 8 例：域名工具（isDouyinCookieDomain / isBlockedExternalHost）、will-navigate 防劫持、外链限流、商店域名拦截、标记即关窗两段式保存
- 全量 462 测试通过

## [1.50.4] - 2026-08-20

### 修复
- **抖音登录后窗口不关闭、配置不保存**：抖音登录/扫码页本身用 `window.open` 打开，上一版把弹出链接一律弹到外部浏览器，导致登录发生在用户浏览器里、应用内嵌会话永远拿不到 cookie——现在**抖音域内的 http/https 弹窗放行在应用内打开**（与登录窗共享会话），仅非抖音域外链走用户浏览器，自定义协议仍静默拦截
- **登录窗显示上次登录账号**：每次「连接抖音」先清空内嵌会话中的旧抖音 cookie，保证打开的是全新扫码页

### 测试
- 新增 4 例：isDouyinHost 判定、抖音域弹窗 allow、连接前清理旧 cookie、弹窗三分类（域内 allow/外链浏览器/自定义协议静默）
- 全量 457 测试通过

## [1.50.3] - 2026-08-20

### 修复
- **弹出链接不再触发 Windows「需要使用新应用以打开此 bytedance 链接」弹窗**：window.open 拦截改为只放行 http/https 链接走用户默认浏览器；`bytedance://` 等自定义协议系统无处理程序，一律静默拦截（此前交给 shell.openExternal 会反复弹系统协议选择框）

### 说明
- 抖音登录窗本身是应用内模态窗口（黑框）：这是「无 Cookie 展示」方案的一部分——Electron 内嵌会话负责捕获登录 cookie，主进程闭环校验；「弹出链接走用户浏览器」规则针对的是页面里的 window.open 链接

## [1.50.2] - 2026-08-20

### 修复
- **抖音登录窗未登录即被关闭**：轮询误把 `sid_guard`（抖音匿名访客 cookie，打开首页即生成）当作登录标记，导致用户在扫码前窗口就被关闭、抓到游客 cookie、随后校验报「登录态校验失败」——现在只认真实登录会话 cookie（sessionid/sessionid_ss/uid_tt/sid_ucp），命中后等待会话落定（1.2s）再抓取整串 cookie，并**调用校验接口确认通过才算登录成功**；校验失败时窗口保持打开继续轮询，不再误关
- **抖音弹出链接未走用户浏览器**：登录窗与主窗口均新增 `setWindowOpenHandler`（deny + `shell.openExternal`），应用内所有 `window.open` 链接统一交给用户默认浏览器打开
- **抖音登录窗被重定向误关**：`did-fail-load` 忽略 ERR_ABORTED(-3)（页面重定向/导航中断时触发），只响应主框架的真实加载失败

### 测试
- douyin-auth 新增 6 例：登录标记判定（sid_guard 排除）、忽略错误码判定、cookie 串拼接、匿名态窗口保持打开直至真实登录、弹出链接 deny+openExternal、-3 不误关
- 全量 453 测试通过

## [1.50.1] - 2026-08-20

### 修复
- **Whisper 下载内存背压**：下载改为流式写盘（write 背压 + drain 等待 + 分块错误监听），1.4GB 压缩包不再整包堆积内存，取消时正确清理临时文件
- 文案统一：安装包体积口径统一为「约 1.4 GB」，默认目录文案统一为「PodMuse笔记」
- 首次启动向导文档口径与实现对齐：每次启动提醒直到完成核心配置或勾选「下次不再提醒」（docs/无感配置方案.md）

### 新增
- **敏感凭据加密扩列**：export.notion.token 与 feishu_oauth.appSecret 纳入 safeStorage 加密（enc:v1 格式、旧明文兼容、不可用回退、不可解密清空）；loadSafeConfig 清空 export.notion.token，新增 notion:exportStatus IPC，设置页显示「已配置不回显」
- **设置页逐字段剪贴板填充**：飞书高级三字段（App ID/Secret/Chat ID）与 Notion 高级两字段（token/database id）支持「粘贴」按钮与自动识别（cli_/oc_/secret_/32 位 hex，带边界防截取，只填空字段不覆盖）
- AI「测试连接」成功后自动加载模型列表（带供应商切换守卫与禁用态）
- 图文指南（GuideCarousel）支持英文展示（manifest 双语 titleEn/captionEn）

### 测试
- 本批新增 21 例（extractFieldValue 11、ai-model-loader 4、加密扩列分支、manifest 英文断言），全量 447 测试通过

## [1.50.0] - 2026-08-20

### 新增
- **Whisper 一键下载**：设置页新增「一键安装 Faster-Whisper-XXL」——自动从 GitHub 官方 tag 选版下载（1.36GB .7z，直连失败自动回退 ghfast/gh-proxy/mirror.ghproxy 镜像）、Windows 自带 bsdtar 解压、自动写回引擎路径；主进程状态机支持后台进度/取消，重进设置页可续看进度；**默认模型 large-v3-turbo**
- **首次启动配置向导（OnboardingWizard）**：AI Key（剪贴板自动填充 + 测试连接成功自动前进）→ 笔记目录（一键默认目录/选择文件夹）→ Whisper（检测/一键下载/跳过）→ 完成页待办卡片（飞书/抖音/Notion 直达设置）；进度持久化续接、「稍后配置」与「不再提醒」；老用户核心已配齐不打扰；每步「不会？看图文」
- **剪贴板无感填充**：新增 clipboard:readText IPC 与 useClipboardFill hook（向导步激活时 1s 轮询、命中即填即测，日志只记命中类型）
- **AI 测试连接**：TabApi 与向导内置「测试连接」按钮（最小 1 token 请求，错误码结构化映射：key 无效/无权限或余额/地址错误/限流/网络，detail 脱敏）
- **一键默认目录**：目录区「一键使用默认目录」自动创建 文档/PodMuse笔记 与 下载/PodMuse音频（仅空字段写回）
- **safeStorage 敏感凭据加密**：douyin_cookie 与飞书/Notion OAuth token 加密落盘（enc:v1 格式，自动解回、旧明文兼容、不可用时回退明文）
- i18n 补齐 44 个英文键；设置页新增「打开在线教程」

### 修复
- 向导「去设置」跳转时先关闭向导遮罩，避免盖住设置弹层

### 测试
- 本批新增 78 例：whisper-downloader 12（含真实 GitHub 资产冒烟与 bsdtar 解压往返）、ai-test 错误码映射、onboarding 状态机 25、config 加密 15、剪贴板匹配等
- 全量 426 测试通过

## [1.49.0] - 2026-08-20

### 新增
- **设置页「连接服务」模式**（方案 docs/配置体系优化落地实现方案.md P0）：
  - 图文说明书内置：新增 GuideCarousel 图文弹层 + 6 组步骤指南（AI Key/飞书/抖音/Notion/Whisper/笔记目录），AI、飞书、Notion、Whisper、目录区均有一键「看图文」；截图缺失时以占位卡优雅降级，后续把 PNG 放入 public/onboarding/<key>/n.png 即零代码补图
  - 抖音「无 Cookie」登录：登录窗扫码后主进程自动捕获并校验 Cookie（校验接口实测：无 cookie 返回 error_code=1 会话过期），设置页只显示「已连接·昵称 / 已过期」状态卡；Cookie 全程不出主进程、渲染层零接触（config:get 置空 + config:save 无条件还原），配置新增 douyin_login 状态
  - 飞书 / Notion OAuth 连接骨架：授权码模式主进程闭环（127.0.0.1 临时回调 server、token 交换与飞书 5 分钟自动续期、群聊/数据库选择器、401 置断开），未注册平台时按钮优雅置灰；手动高级模式折叠保留，notion 导出优先使用 OAuth 凭据
- i18n 新增连接服务相关文案（含英文翻译）

### 修复
- 抖音登录窗子资源加载失败不再误关窗口（仅主框架 did-fail-load 才中止）

### 测试
- 新增 75 例：douyin-auth 27（校验四分支/状态流转）、oauth-client 39（token 交换/续期/断开/未配置降级）、guide-carousel 9（manifest 与降级）
- 全量 348 测试通过

## [1.48.2] - 2026-08-19

### 修复
- **智谱（zhipu）baseUrl 归一化 bug**：预设地址 /api/paas/v4 被机械追加 /v1 成未文档化的 /v4/v1 路径——新增统一 `normalizeBaseUrl`（对 /v1、/v4 等版本段结尾不再追加），AI 调用/模型列表/批量任务模型覆盖全部走统一函数，官方端点 /v4/chat/completions 正确拼接
- Notion 导出：新增空凭据前置校验；修复无 frontmatter 时标题 fallback 失效（此前真实导出会被 Notion 400 拒绝）
- AI Key 校验 trim：纯空格 key 不再被当作已配置

### 测试
- 新增 AI 供应商测试 32 例：/v1 归一化全边界、版本化路径、无 key 优雅失败、任务级模型覆盖、QA 无 key 可读错误
- 新增 Notion 集成测试 25 例：请求构造（URL/鉴权头/signal）、导出成功链路、空凭据、>100 块截断、错误传播
- 全量 271 测试通过；真实连通性矩阵：6 个非 DeepSeek 供应商全部可达且鉴权路径正常（假 key 401），无网络阻断

## [1.48.1] - 2026-08-19

### 修复
- **英文实体卡片链接显示异常**：文件名含空格的实体（Five Guys、M Stand、Apple Intelligence、DeepSeek Harness 等）生成的 `[名称](../项目/Five Guys.md)` 不符合 CommonMark 规范，渲染器无法解析成链接而显示为原始文本（中文名无空格所以正常）——现在生成链接时统一编码空格/#/?/半角括号（%20 等），并兼容旧版未编码链接避免重复追加
- 修复 AI 输出残留的异常 wiki 片段（如 `[张雪](../人物/张雪.md)项目/张雪机车]]`、`[DeepSeek](../项目/DeepSeek.md)pSeek Harness]]`）：wiki 链接转换支持目录前缀（`[[项目/张雪机车]]`），未收录名称降级为纯文本不再生成坏链接
- 已按新规则修复笔记库中受影响的播客笔记（汉堡开店潮 / Vans 匡威 / 不爱直播带货 / 大脑可塑性）

### 改进
- **悬浮卡片深色模式可读性**：预览浮层改用不透明主题背景（原深色模式背景仅 6% 白，会透出底下内容导致看不清）
- **悬浮卡片交互**：同一链接第二次悬浮不再失效（关闭时未重置预览标记导致被短路）；关闭仅发生在鼠标真正移出浮层边缘（含 250ms 边缘宽限），从链接移入浮层有 420ms 缓冲，不再「随便动一下就关掉」

## [1.48.0] - 2026-08-19

### 新增
- **支持 Fireside 播客平台**：新增 Fireside 适配器，直接粘贴节目页链接（如 guiguzaozhidao.fireside.fm/20240440）即可处理——自动解析页面的 og:audio:secure_url/og:audio/JSON-LD/<audio> 四级回退提取音频直链与 og:title 标题（含 HTML 实体解码），去重 key 用节目域+节目 id，跨节目唯一
- 飞书消息链接识别新增 fireside 节目页模式（自动排除 feeds.fireside.fm RSS 链接）

### 修复
- 订阅 fireside.fm 播客自动入队报「暂不支持该平台，请使用本地文件方式」：入队 source 现按平台适配器智能选择——页面链接有适配器优先用页面链接，否则回退 RSS 的 enclosure 媒体直链（自动/手动两条入队路径都覆盖），标题继续随任务传递
- 旧失败记录自愈：装上新适配器后，历史页对失败记录点「重新处理」即可直接重试成功

## [1.47.4] - 2026-08-19

### 修复
- **订阅批量处理链路：标题与状态不一致（历史记录显示「已暂停」且缺标题）**
  - 根因 1：30 秒一致性巡检不识别批量队列为「活跃进程」，把处理中的批量任务提前标成「已停止」移出活跃列表，导致随后的完成/失败写入静默丢失——现在批量处理期间巡检不再误判
  - 根因 2：完成/失败/停止的状态写入只查活跃列表，任务被移走后无法回填——现在按任务身份跨列表精确定位并回填终态（状态/标题/文件名）
  - 标题链路补齐：订阅自动拉取与手动入队时携带 RSS 解析出的标题（入队即显示，处理前不再「识别标题中」），失败分支同样回填已获取的真实标题
  - 启动对账：历史记录中与批量队列终态不一致的旧条目（已完成却显示已停止/无标题）自动修复回填
- 单任务（URL 粘贴）处理同样回填平台适配器返回的真实标题，预取失败时历史记录不再丢标题

## [1.47.3] - 2026-08-15

### 改进
- 应用图标全面焕新：全新紫色渐变 squircle 图标（白色 P 形 + 播放三角 + 波形条），同步替换安装包图标（icon.ico/icon.png）、侧边栏与关于对话框 logo

## [1.47.2] - 2026-08-15

### 修复
- 去掉分享图左下角紫色小图标（来源装饰，纯视觉噪音）
- **AI JSON 宽容解析**：AI 输出带「好的，以下是 JSON：」等包装文字时也能正确解析（之前解析失败导致整张卡回退成原文要点+原标题）
- AI 未生效时系统通知明确告知原因（未配置模型 / AI 失败 / 未生成标题），不再静默回退

## [1.47.1] - 2026-08-15

### 修复
- **分享图高度随内容自适应**：宽度固定 1080，高度按内容量伸缩（最短 1080px，最长 2560px）——不再固定 1440，彻底解决金句框/内容被截断
- AI 输出稳定性：temperature 0.7 → 0.3（同一篇笔记多次生成结果一致）
- 类型判断强化：六类型明确判据（对比/方法步骤/金句/框架/海报/知识总结），不再默认全归 summary
- 知识点 4 条 → 3 条 + 标题/摘要行数收紧（防内容超出版面）

## [1.47.0] - 2026-08-15

### 新功能
- 分享卡版式扩展到 **6 种**，AI 按内容类型自动选择：
  1. **知识总结**（summary）：标题+摘要+编号知识点+金句框
  2. **金句卡**（quote）：极简居中大金句+摘要
  3. **方法步骤**（steps）：圆形编号步骤流
  4. **对比分析**（compare，新）：双列对比卡（左灰右紫，如「普通做法 vs 高效做法」）
  5. **框架流程**（framework，新）：纵向节点流程图（首节点紫色高亮+箭头连接）
  6. **极简海报**（poster，新）：超大标题+紫横线+点睛副句，海报式留白
- AI 提示词升级：六类型判断 + 对比数据提取（对比项名称+两侧特点列表）

## [1.46.1] - 2026-08-15

### 修复
- 金句版分享卡：删掉标题上方多余的装饰性大引号（与内容脱节的视觉噪音）
- 修复金句模板字段错位：主角改为 AI 提炼的**金句本身**（quote 字段），摘要作为副行——之前误把传播标题当金句展示
- 知识总结版的金句框同步去掉装饰引号

## [1.46.0] - 2026-08-15

### 新功能
- 分享图全面重做为 **AI 知识卡片**：不再是「PodMuse 宣传页」，而是「内容杂志」风格（米白底 + 黑正文 + 细线分割 + 大量留白，品牌退到小字第二层）
- **AI 内容编辑 pipeline**：生成前 AI 一次完成 ①判断内容类型（知识总结/金句/方法论）②生成传播标题（不是检索式原标题，观点型/冲突型/总结型）③一句话摘要 ④3~4 条知识点（小标题+说明）⑤金句提炼
- **3 种版式按内容自动选择**：知识总结型（标题+摘要+编号知识点+金句框）/ 金句型（极简居中大金句）/ 方法步骤型（圆形编号步骤流）
- AI 不可用/失败时回退规则提取（原文要点 + 原标题），生成永远可用

## [1.45.5] - 2026-08-14

### 新功能
- 分享卡三点总结 AI 化：生成分享图时，三条知识点改为 **AI 钩子式总结**——基于笔记内容提炼「让人一眼就想了解这篇笔记」的短句（带数字/事实/反差，每条 ≤28 字），替代原来照抄原文的规则提取
- AI 不可用/失败时自动回退原有规则提取（关键数据→金句→段落），分享卡生成永远可用

## [1.45.4] - 2026-08-14

### 修复
- 剪藏浮窗空壳的**真正根因**：clip-toast.html 内联脚本中误用了 TypeScript 的 `as` 类型断言语法（纯 HTML/JS 页面不支持），导致整个脚本语法错误、init() 从未执行——标题、按钮文字全部没有渲染。改为纯 JS 语法后正常
- （v1.45.3 的非透明窗口改动保留，作为渲染稳定性兜底）

## [1.45.3] - 2026-08-14

### 修复
- 剪藏浮窗仍是空壳（按钮只有色块没有文字）：根因是 Windows 上 `transparent + focusable:false` 窗口在部分显卡合成器下内容不渲染——改为**非透明窗口**（背景色跟随系统深/浅色），渲染绝对稳定

## [1.45.2] - 2026-08-14

### 修复
- 剪藏浮窗只剩「条状物」：彻查后修复两个渲染问题
  - 浮窗高度 108px 不够容纳内容（标题 2 行 + 域名 + 按钮行 ≈ 115px），底部按钮被裁掉——加高到 156px
  - 浮窗右下角定位在部分多屏/DPI 缩放下会算偏导致窗口大半跑出屏幕外只露一条边——位置 clamp 到工作区内

## [1.45.1] - 2026-08-14

### 改进
- 打赏弹窗收款码放大并保持原始比例（220px 宽 + 弹窗加宽到 520px，此前 170px 方形压缩导致二维码不清晰）
- README 精细化重写：换掉「写入 Obsidian」表述（改为「沉淀到自己的笔记库，兼容 Obsidian/Logseq 等」）、增加「为什么做 PodMuse」、功能按类别分组、英文翻译版紧随中文介绍之后

## [1.45.0] - 2026-08-14

### 新增
- 打赏入口：侧边栏「关于」下方新增「☕ 请我喝杯咖啡」按钮（紫色强调），点击弹出打赏弹窗——委婉文案 + 微信/支付宝收款码并排展示
- README 全面重写：中英双语、按项目最新状态重新组织（核心功能表 / 快速开始 / 技术栈 / 项目结构 / 打赏区）

## [1.44.2] - 2026-08-14

### 修复
- 历史页弹层全部失效/被裁剪问题（根因：弹层 absolute 定位在 overflow:hidden 祖先内被裁掉）：
  - 导出菜单与「更多」菜单全部改为 **fixed 定位 + portal 渲染到 body**（z-index 99999），不再受列表列容器、详情滚动区裁剪——列表与详情内点击均正常弹出
- 详情面板宽度随窗口自适应：280px 固定 → `clamp(240px, 26%, 320px)`（设计稿规范），窗口缩放时详情同步变化

## [1.44.1] - 2026-08-14

### 修复
- 历史页表格对齐重设计：
  - 数据行改为与表头一致的 6 列布局（复选框 / 任务信息 / 状态 / 平台 / 创建时间 / 操作），各列不再挤在一起
  - 统计卡片 grid 4 列 → 5 列（此前第 5 张卡「本周新增」挤到第二排）
  - 详情操作区精简为 [查看笔记] [导出] [⋯]（复制链接/删除收进更多菜单，按钮文字不再挤出）
  - 详情「更多」弹层改向下弹出（向上弹出面板导致位置飘到列表区）

## [1.44.0] - 2026-08-14

### 新增
- 历史页按设计稿继续升级（对齐效果图）：
  - 统计卡片 4 → 5 张：总任务 / 已完成 / 处理失败 / **已生成笔记** / **本周新增**，每张卡配彩色圆形图标（紫/绿/红/蓝/橙）
  - 任务列表改为**表格样式**：表头（任务信息 / 状态 / 平台 / 创建时间 / 操作）+ 数据行
  - 新增**排序选择器**（最新创建 / 最早创建）
  - 新增**分页栏**（共 N 条记录 + 页码导航，每页 10 条）
  - 任务详情面板增强：右上角**关闭按钮**、**笔记字数**（自动读取）、**复制链接**按钮、**删除任务**按钮

## [1.43.6] - 2026-08-14

### 修复
- 历史页 B 站图标未生效：平台名是「B 站」（带空格），图标匹配表是「b站」（无空格）导致匹配失败——归一化去空格后命中
- 全屏处理时流程面板步骤间连接线很短、两侧大片空白：连接线固定宽度（≤32px）不随轨道拉伸——改为 flex 拉伸填满节点间距，最后一个节点不占拉伸空间

## [1.43.5] - 2026-08-14

### 修复
- 重处理已处理过的播客不再重新下载音频/转写（根因：超长标题被 Windows 文件名上限截断，重处理时按完整标题算的文件名与磁盘上的截断文件名不匹配 → 缓存永远命中不了）：
  - 缓存文件名改为「标题前 80 字 + 内容 ID（B站 bvid 等）前 8 位」——短且稳定，同一节目永远命中
  - 兼容旧缓存：新文件名不存在时按标题前缀查找历史截断文件（已有的音频/转写缓存继续复用）
- 历史页任务详情 UI：
  - 「导出」图标空白：详情面板导出按钮与文字按钮样式冲突，改用专用图标按钮样式
  - 「更多」菜单弹层被详情框 overflow 裁剪导致位置错乱：移除裁剪，弹层正常浮出
  - 详情框改为 flex 占满侧栏高度，与列表齐平无空白

## [1.43.4] - 2026-08-13

### 修复
- 浏览器剪藏浮窗「加入队列」点击反馈：入队成功立即显示「已加入队列」并 1.2 秒后关闭（此前 batchStart 失败时吞掉反馈、窗口不关）；关闭兜底双通道（IPC 失败再 window.close）
- 浮窗按钮加按压/悬停动效（scale 0.94 + 主按钮高亮）

### 改进
- B 站图标升级为 dashboard-icons 官方彩色版（512px，用户提供来源）；YouTube/Apple Podcasts 同步升级同库彩色版

## [1.43.3] - 2026-08-13

### 修复
- 重复打开软件时主进程崩溃（EADDRINUSE 127.0.0.1:41987）：
  - 单实例锁失败时 app.quit() 是异步的，第二个实例仍会走完初始化 → 浏览器扩展端口冲突崩溃；改为 app.exit(0) 同步立即退出
  - clip-server 增加 EADDRINUSE 容错：端口被占（极端残留场景）时降级不崩主进程，扩展剪藏功能静默关闭

## [1.43.2] - 2026-08-13

### 修复
- 活跃任务列表/批量处理面板仍显示链接：两层修复
  - 预取：fetchPodcastTitle 智能升级——B 站等有适配器的平台优先用平台专用 API（B 站 view API）拿标题，og:title 仅兜底（此前 B 站预取被风控拒绝 → 任务全程显示 URL）
  - 渲染兜底：全部渲染点（活跃任务卡片/批量队列行/报告表格/最近任务/历史列表行）绝不显示链接——标题缺失或为 URL 时按文件名 → 「识别标题中…」（处理中）→ 「未命名任务」降级
- 历史页任务详情按约定显示原始链接（标题下方灰色小字）

## [1.43.1] - 2026-08-13

### 修复
- B 站任务标题显示为链接地址：处理完成后从平台 API 拿到的真实标题不回填历史记录（预取标题失败时一直保持 URL）。修复：processPodcast 回传实际标题 → 任务完成后覆盖 task.title 并写入历史记录。历史中的旧 URL 标题任务，重新生成一次即可显示真实标题

### 改进
- 历史页平台封面改用官方高清图标：B站/小宇宙/喜马拉雅/抖音/YouTube/Apple Podcasts（矢量 SVG 或 128px PNG，白底圆角），无图标的平台用首字占位

## [1.43.0] - 2026-08-13

### 重构
- 历史页面按新版设计稿 UI 重构：
  - 统计卡片 4 张横向铺满：总任务 / 已完成（含成功率）/ 处理失败 / 平台分布条形图（动态计算）
  - 新增搜索框（标题/链接/平台）+ 状态/平台/时间三级筛选
  - **左右双栏布局**：左侧任务列表（约 75%）+ 右侧 280px 任务详情（封面/标题/状态/时间/失败原因/笔记状态 + 查看笔记/下载/更多），上下齐平，未选中时显示引导
  - 紧凑任务行：48×48 平台封面 + 标题（最多两行）+ 平台徽标 + URL 降级为灰色辅助 + 状态徽章（淡绿/淡红）+ 自然时间显示（今天 HH:mm / 昨天 HH:mm / MM-DD HH:mm，悬停完整时间）
  - 操作收敛：行内 [预览] [导出] [···]（失败任务加 [重新生成]），更多菜单收纳查看详情/打开笔记/复制链接/重新生成/删除
  - 点击行选中（淡紫背景）+ 右侧详情即时更新，无弹窗
  - 空状态与搜索无结果引导
  - 不再展示实时进度（职责归工作台）

## [1.42.2] - 2026-08-13

### 改进
- 历史页行操作布局优化：常用操作改为带文字的按钮（预览 / 重新生成 / 更多），打开、打开所在文件夹、删除收进「更多 ⋯」菜单——不再是一排看不懂的裸图标，新用户一眼明白每个按钮的作用

## [1.42.1] - 2026-08-13

### 修复
- PDF/合集导出标题错误：标题提取误取笔记第一个 `# 一句话总结` 模块标题，导致所有导出的笔记标题都显示「一句话总结」——修复为优先用笔记文件名，正文提取时跳过固定模块标题（一句话总结/本期主要内容/核心观点/事件详情/金句摘录/术语词典等）

## [1.42.0] - 2026-08-13

### 新增
- 统一导出中心（历史页 + 批量处理报告卡片共用同一套导出菜单）：
  - 分享图 PNG（原有）
  - 导出 PDF（单篇笔记 A4 排版，含页码）
  - 导出 Markdown（笔记原文另存，自选路径）
  - 导出到 Notion / Logseq（原有能力统一入口）
- PDF 合集导出：历史页勾选 2 篇以上笔记 → 一键生成「封面 + 目录 + 逐篇分页」的合集 PDF（适合打包阅读/打印/分享）
- 技术：marked 渲染 → 隐藏窗口 printToPDF（A4 打印样式），零新增依赖

## [1.41.7] - 2026-08-13

### 修复
- B 站视频「提炼笔记」报 finish_reason=length（AI 输出被 max_tokens 截断，甚至 content 为空）：
  - 分段处理阈值 30000 → 16000 字符（更长内容自动分段生成，每段输出量小不易截断）
  - 单次生成 max_tokens 16384 → 24576（推理模型 reasoning 吃掉预算后仍有足够空间输出笔记）
  - 输出被截断时自动重试一次，并提示 AI 精简输出（要点 ≤8 条、事件详情 ≤3 个、卡片每类 ≤4 个）；content 为空 + length 截断同样自动重试

## [1.41.6] - 2026-08-13

### 修复
- 批量处理报告里的笔记预览弹窗被卡片挡住、滚轮只能滚底层：根因是批量面板根元素是 motion.div（带 transform），导致弹窗的 position: fixed 失效被限制在面板内——改为 createPortal 挂到 body（与更新弹窗同款方案），弹窗现在全局置顶、滚动只在弹窗内生效

## [1.41.5] - 2026-08-13

### 修复
- 笔记链接清洗（normalizeEntityLinks）：AI 偶发输出的 `[概念/化学性改变](概念/化学性改变.md)` 这类链接——文本带「概念/」前缀、路径缺 ../ 导致子目录笔记链接失效——保存前统一清洗：
  - 链接文本去掉目录前缀（[概念/脑机接口] → [脑机接口]）
  - 链接路径按笔记实际目录重算相对路径（任意目录深度都正确）
- 实测大脑可塑性笔记：残留带前缀链接 0 个，Michael Merzenich 等人名链接不受影响

## [1.41.4] - 2026-08-13

### 修复
- B 站等长视频「提炼笔记」超时（The operation was aborted due to timeout）：AI 单次请求超时从 120 秒加长到 300 秒（长文本提炼可达数分钟，120s 频繁误杀）
- 超时重试耗尽后的错误改为可读中文提示（原为英文 undici 错误）

## [1.41.3] - 2026-08-13

### 修复
- 笔记生成质量门：
  - AI 输出缺 frontmatter 或实体链接覆盖不足时自动重新生成一次（提示 AI 严格输出 frontmatter 与 [[链接]]）
  - 仍未输出 frontmatter 时后端自动补全（B站/YouTube 默认「科技商业」分类）——解决 B 站视频笔记不自动分文件夹的问题
- 正文补链接兜底（linkifyBody）：卡片区存在的实体，正文出现但漏链的自动补 [[目录/名称]] 链接（跳过已有链接、跳过英文词内部、长名优先）——解决「卡片有解释、正文没链接上」的问题

## [1.41.2] - 2026-08-13

### 修复
- 订阅页只占窗口一半：去掉 .sub-view 的 max-width: 860px 限制，订阅视图随窗口全宽自适应

## [1.41.1] - 2026-08-13

### 改进
- 分享卡片内容改为「用户需要关注的信息」：
  - 要点按信息密度提取：优先「深度解读」章节的关键数据（如 618 卡诗领跑/防脱是唯一稳住价格的品类/护发精油增速第一）→ 金句 → 动态列表兜底
  - 实体标签改为匹配标准 Markdown 链接 `[名称](../目录/名称.md)`（此前只匹配 wiki 语法导致 chips 全空），并按类型加权排序：公司/品牌/人物优先于概念术语
  - 卡片日期优先取笔记 frontmatter 的 date（无则当天）
- 保存分享图后不再自动打开文件夹（用户自选路径已足够）

## [1.41.0] - 2026-08-13

### 新增
- 分享卡片：一键把「本期笔记」生成 1080×1440 竖版分享图（品牌紫深色版式：logo/标语、节目标题、播客名与平台徽标、3 条要点、实体标签 chips、生成日期）
- 保存路径由用户选择：生成后弹保存对话框（默认桌面 + 带日期文件名，可改路径），保存后自动打开所在文件夹
- 分享入口：历史页成功笔记行 + 笔记预览弹窗头部
- 技术：隐藏 BrowserWindow 渲染 HTML 模板 → capturePage 截图，零新增依赖

## [1.40.0] - 2026-08-13

### 新增
- 历史页资产视图改造：
  - 顶部统计卡：累计笔记数 / 本周新增 / 成功率 / 平台分布条形图（只统计成功产出的笔记）
  - 列表默认只显示成功笔记（「仅成功笔记 / 全部任务」可切换），失败任务可在全部模式查看与重跑
  - 新增「打开所在文件夹」操作
- 重新生成可选模型：点击重新生成时弹出模型选择（列出已配置 API Key 的所有供应商与可用模型，标注当前模型），选中的模型只作用于该任务（批量队列支持任务级模型覆盖）

## [1.39.1] - 2026-08-13

### 修复
- 喜马拉雅订阅 406 失败：rss-parser 默认 UA（rss-parser）被喜马拉雅反爬拒绝，改为浏览器 UA + 兼容 Accept 头（实测 13 个源全部可解析）
- 订阅后任务反复爆发：已见集合改每源独立（旧全局 500 上限被多源挤兑，老源历史被挤掉后重新入队）；每个源只跟踪最近 200 条，超长 feed（喜马拉雅 1000+ 期）不再触发历史节目反复入队

## [1.39.0] - 2026-08-13

### 新增
- 处理历史页（侧边栏「历史」入口）：历史记录从 5 条扩容到 500 条
- 支持按平台（小宇宙/B站/YouTube/喜马拉雅/Apple Podcasts 等）与时间范围（今天/近 7 天/近 30 天）筛选
- 历史任务操作：预览笔记 / 打开笔记 / 一键重新生成（按当前配置重新入队处理）/ 删除 / 清空历史（二次确认）
- 空历史显示引导「还没有处理记录」+ 去处理播客按钮

## [1.38.0] - 2026-08-13

### 改进
- 侧边栏默认展开：启动 PodMuse 时左侧侧边栏默认打开（完整宽度），不再默认收缩成图标窄栏

## [1.37.0] - 2026-08-13

### 修复
- 批量处理报告操作列按钮错位：列宽 80px 改为 150px，操作按钮改 flex 居中排列（预览/打开不再挤压错位）
- 笔记预览弹窗滚轮穿透：弹窗内滚动到边界或非滚动区域时拦截 wheel 事件，下层卡片不再跟着滚

## [1.36.0] - 2026-08-13

### 修复
- 剪藏浮窗「忽略」按钮失效：transparent 窗口内 window.close() 不可靠，改为 IPC 让主进程关闭
- 剪藏浮窗阴影异常：去掉 box-shadow（transparent 窗口无法正确渲染阴影）

### 改进
- 浏览器扩展图标换为 PodMuse 应用图标（`build/icon.png` 缩放的 16/32/48/128 全套）

## [1.35.0] - 2026-08-13

### 新增
- 浏览器扩展（Chrome/Edge 本地加载版）：工具栏图标 / 右键菜单一键发送当前页面到 PodMuse，仓库 `chrome-extension/`，安装包 `dist-chrome-extension/podmuse-clipper.zip`
- 本地接收服务：PodMuse 监听 127.0.0.1:41987 接收扩展请求，仅放行 chrome-extension:// Origin（防恶意网页塞链接）

## [1.34.0] - 2026-08-13

### 改进
- 书签小工具安装指引改为 Chrome/Edge 兼容步骤：Ctrl+D 收藏任意网页 → 编辑书签网址替换为代码（Chrome 不允许新建 javascript: 书签，但允许编辑已有书签网址）

## [1.33.0] - 2026-08-13

### 新增
- 浏览器剪藏：复制播客/视频/RSS 链接即弹右下角浮窗，一键加入处理队列或订阅（10 秒去重、已处理链接提示、不抢焦点、可关闭）
- podmuse:// 协议 + 书签小工具：浏览器书签一键发送当前页面到 PodMuse（设置 → 工具维护可复制代码）
- 设置 → 工具维护：「剪贴板链接检测」开关（默认开启）与「复制书签小工具」按钮

### 安全
- 剪贴板内容仅在本机内存判断，不落盘不上传

## [1.32.0] - 2026-08-13

### 改进
- 问答页无笔记引导：检测到笔记库为空时，不再展示无意义的示例问题，改为友好提示「先处理一个播客，AI 转写并生成结构化笔记后，就可以向知识库提问了」+ 「去处理播客」按钮一键跳回工作台

## [1.31.0] - 2026-08-13

### 新增
- 问答界面示例问题动态化：基于笔记库高频实体（人物/概念/项目/术语）自动生成个性化示例，如「哪几期播客提到过{人物}」；无笔记时回落默认示例

## [1.30.0] - 2026-08-13

### 修复
- 订阅后突然涌入大量任务：根因是首次检查把 RSS 全部历史节目当新节目入队。现在首次订阅只处理最新 1 期，其余历史节目标记已见，之后仅增量处理新发布节目
- 窄窗口顶栏按钮文字竖排：刷新/语言/主题按钮加 nowrap + 不收缩

### 改进
- 顶栏去掉「30s 轮询 · Obsidian: 小宇宙播客」多余文案

## [1.29.0] - 2026-08-13

### 修复
- 订阅后推荐区消失：推荐订阅改为始终显示（订阅列表上方），已订阅内容标记「已订阅」，可随时继续添加
- 更新弹窗挤在侧边栏：根因是侧边栏 backdrop-filter 使 fixed 定位失效，弹窗改用 Portal 挂载到 body，全屏居中显示

### 改进
- 推荐模块折叠态不再空白：每个平台模块为卡片容器，折叠时横向展示 6 个预览卡片 + 「查看全部 (N)」按钮，点击展开完整网格

## [1.28.0] - 2026-08-13

### 修复
- 小宇宙订阅 406/403：官方 rsshub.app 实例拒绝小宇宙路由，默认实例切换为国内镜像 rsshub.rssforever.com，并支持多实例自动故障转移（配置实例 → rssforever → rsshub.app）

### 移除
- YouTube 推荐模块与镜像实例配置（国内直连不可用，按用户要求移除）

### 新增
- 推荐内容扩充至 63 源并重组：小宇宙（38 个中文播客）/ 喜马拉雅（13 个，album XML 公开可访问）/ Apple Podcasts（12 个英文播客），全部实测有效

## [1.27.0] - 2026-08-13

### 修复
- YouTube 频道订阅超时报错不友好：超时错误现在给出明确指引（直连 YouTube 需代理网络，或配置镜像实例），不再显示原始英文错误

### 新增
- 推荐订阅模块改为折叠式：默认收起只显示平台标题行（图标+名称+数量），点击展开具体播客卡片
- 订阅设置新增「YouTube 镜像实例」：填入 Invidious 实例地址后，YouTube 频道订阅自动走镜像（留空为直连）

## [1.26.0] - 2026-08-13

### 修复
- 订阅页搜索框紫色双框：根因是全局无障碍规则 `input:focus-visible` 的紫色 outline 叠加在输入框上（主界面 UrlInput 早已豁免，订阅页漏了），已豁免并与主界面输入框样式统一

### 新增
- 推荐订阅按平台模块化展示：小宇宙（25 个中文播客）/ Apple Podcasts（12 个英文播客）/ YouTube（5 个科技频道），共 42 个内容源（feedUrl 全部实测有效）

### 改进
- 订阅页搜索框换主界面 UrlInput 同款样式：44px 高卡片式、聚焦时 accent 边框 + 柔光圈、图标联动变色

## [1.25.0] - 2026-08-13

### 新增
- 自动更新（增量）：electron-updater + GitHub Releases，无需再手动下载安装包
  - 启动后 10s 自动检查 + 每 6 小时（仅拉取 latest.yml 元数据，约 1KB）
  - 有更新时侧边栏版本号高亮 + 显示「(1)」，点击打开更新面板（Hermes 式交互，不弹横幅）
  - 点击「下载更新」才开始下载（blockmap 差分，只下变化部分），默认不自动下载
  - 下载进度条 → 完成后「重启并安装」静默升级（配置与订阅数据保留）
  - 失败回退：检查失败静默；下载/安装失败提示 + 手动下载跳转 Releases
  - 设置 → 工具维护：自动检查更新（默认开）、发现更新后自动下载（默认关）
  - 便携版与开发模式自动禁用更新检查

## [1.24.0] - 2026-08-13

### 新增
- 智能订阅：用户无需知道 RSS 地址即可订阅播客
  - 订阅升级为侧边栏一级入口（工作台之后），原设置内「订阅」tab 移除
  - 智能输入框：输入播客名自动搜索（iTunes Search API，免费），一键订阅；粘贴链接自动解析（Apple Podcasts / 小宇宙经 RSSHub / YouTube 频道 / 喜马拉雅专辑 / 普通网页 feed 自动发现）
  - 内置推荐订阅：14 个精选中文播客（feedUrl 已验证有效），空状态冷启动一键订阅
  - OPML 导入：从其他播客 App 迁移订阅，预览勾选批量导入
  - 失败兜底：解析失败给出各平台获取指引 + 手动添加 RSS 通道保留
  - 订阅设置：检查间隔与 RSSHub 服务地址可配置（默认 rsshub.app）

### 改进
- 设置页精简：订阅管理移出设置，成为独立视图

## [1.23.0] - 2026-08-13

### 新增
- 订阅自动抓取：设置页新增「订阅」tab
  - 添加/删除 RSS 订阅源（校验有效性），小宇宙/Apple Podcasts/YouTube 频道 RSS 均支持
  - 定时检查新节目（默认每 6 小时 + 启动时检查），新节目判定持久化防重复
  - 每个源独立策略：自动（新节目直接进批量队列）/手动（待处理列表勾选后加入队列）
  - 订阅源卡片：上次检查时间、累计处理数、新节目列表、原链接跳转

## [1.22.8] - 2026-08-13

### 改进
- 收缩态悬停 App logo：logo 图标切换为展开侧边栏图标（点击展开），更符合 ChatGPT 交互
- 任务概览仅在展开态显示，收缩态完全隐藏
- 点击导航/设置/关于按钮不再自动展开侧边栏，保持收缩状态；仅展开按钮可展开

## [1.22.7] - 2026-08-13

### 修复
- 收缩态图标位置错误：文字标签 opacity 隐藏仍占布局宽度导致图标偏移，改为 display:none 彻底不占位，图标正确居中
- 收缩态悬停 App logo 浮现圆形展开按钮，点击展开侧边栏（ChatGPT 同款交互）

## [1.22.6] - 2026-08-13

### 改进
- 侧边栏改为 ChatGPT 式交互：默认收缩为 64px 图标窄栏（只显示图标），悬停有 tooltip，点击图标切换视图并展开完整侧栏（带文字 + 任务概览 + 版本），展开态顶部有收起按钮

## [1.22.5] - 2026-08-13

### 改进
- 标签栏圆角胶囊化设计 + 每标签专属颜色：按笔记名哈希生成色相，彩色圆点标识，激活标签带同色背景高亮，关闭按钮同色系

## [1.22.4] - 2026-08-13

### 新增
- 阅读器多标签页（Obsidian 式）：点击笔记新开标签，标题栏横向排列可切换/关闭，重复打开同一笔记自动聚焦已有标签
- 阅读器标题栏去掉路径显示，只保留笔记名

## [1.22.3] - 2026-08-13

### 新增
- 笔记库三栏可拖动分隔条：拖动调整文件树/聊天栏宽度（150-420px / 220-520px）
- 文件树和聊天栏支持折叠（收起为细条，点击展开），最大化阅读器空间

## [1.22.2] - 2026-08-13

### 新增
- 笔记库右侧新增聊天侧边栏（三栏布局：文件树 + 阅读器 + 问答），边读笔记边提问
- 聊天回答的引用来源点击后直接在笔记库阅读器中打开

## [1.22.1] - 2026-08-13

### 其他
- docs 产品流程文档整体移出 Git 仓库（本地保留，不上传 GitHub），README 同步移除引用

## [1.22.0] - 2026-08-13

### 新增
- AI 知识问答：侧栏新增「问答」tab，与知识库对话
  - 关键词检索（bigram 分词）召回相关笔记片段，DeepSeek 生成带引用来源的回答
  - 流式输出（逐字渲染）、引用来源可点击打开笔记、示例问题引导
  - 检索层抽象为 Retriever 接口，后续可插拔升级本地向量检索（语义搜索）
  - 模型跟随设置中的当前配置

## [1.21.18] - 2026-08-12

### 改进
- 非知名人物（主持人/嘉宾/博主等）不再生成链接和卡片：
  - AI prompt 明确要求只包裹有公众知名度的人物，主持人/嘉宾等用纯文本
  - 后端兜底：被 `filterNonNotablePeople` 过滤的人物，正文中的 `[[链接]]` 自动转为纯文本，也不补概念卡片
  - 旧笔记渲染层：wiki: 链接指向不存在的卡片（如被过滤的主持人）时降级显示为纯文本，不再出现"笔记不存在"的悬挂链接

## [1.21.17] - 2026-08-12

### 修复
- 旧笔记中带空格的英文人名（如 `[[Michael Woldridge]]`）不显示为链接：转换生成的 `wiki:` 协议 URL 含空格，marked 拒绝渲染。现在对链接目标做 encodeURIComponent，空格编码为 %20 后正常渲染

## [1.21.16] - 2026-08-12

### 修复
- 旧笔记（wiki-link 转换的 `wiki:` 协议链接）不着色：着色逻辑未处理 `wiki:` 前缀，名称提取失败导致全部显示默认色。现在先剥离前缀再查类型映射

## [1.21.15] - 2026-08-12

### 修复
- 旧笔记（wiki-link）悬停预览仍显示「笔记不存在」：全局文件名查找的 key 不匹配（map 存无 .md 的名字，查询带 .md）。现在 wiki-link 转换为 `wiki:名称` 协议，直接按文件名全局解析，一次命中
- 相对链接解析失败时也增加按文件名全局兜底

### 新增
- 链接按实体类型着色：人物紫色、概念黄色、项目绿色、术语灰色（与知识关联面板类型色一致）

## [1.21.14] - 2026-08-12

### 修复
- 预览浮层闪烁：浮层弹出位置可能覆盖链接本身导致 mouseleave/mouseenter 循环。现在浮层优先显示在链接下方，空间不足时显示在上方（绝不覆盖链接）；同一链接浮层已显示时不重复加载；关闭延迟 300ms 缓冲
- 旧笔记（Obsidian wiki-link `[[名称]]` 格式）不显示链接：渲染前自动转换为标准 Markdown 链接，并按文件名全局查找（Obsidian 全局解析语义），旧笔记现在也能悬停预览和跳转

## [1.21.13] - 2026-08-12

### 修复
- 笔记库链接悬停预览全部显示「笔记不存在」：marked v18 会把中文路径 URL 编码（`../项目/x.md` → `../%E9%A1%B9...`），解析前未解码导致路径找不到。现在先 decodeURIComponent 再解析
- 链接解析改为候选列表逐个尝试（相对当前笔记目录 → 库根目录 → 去 `../` 前缀），兼容 AI 生成时漏写 `../` 的链接（Obsidian wiki-link 全局解析语义）

## [1.21.12] - 2026-08-12

### 重构
- 笔记库面板全面重做：
  - 修复悬停预览浮层定位失效（motion transform 影响 fixed 定位，改用 portal 渲染到 body）
  - 文件树改为 Obsidian 风格：默认全部折叠、分组带类型图标、缩进引导线、当前项高亮
  - 阅读器样式完善：标题/引用/代码块/表格完整渲染，正文 660px 居中
  - 预览浮层带标题栏 + 关闭按钮 + 加载状态

## [1.21.11] - 2026-08-12

### 修复
- 笔记库悬停预览不生效：Windows 反斜杠路径下链接解析失败（lastIndexOf('/') 返回 -1），已统一路径分隔符
- 文件列表默认折叠实体卡片目录（人物/项目/概念/术语），播客分类目录展开，分组排序更清晰
- 文件列表收窄（240px → 190px），正文 padding 与 max-width 调小，小窗口下正文不再被挤出

### 其他
- docs/pm 与优化待办文档移出 Git 仓库（本地保留）

## [1.21.10] - 2026-08-12

### 新增
- 笔记库阅读器：左侧导航新增「笔记库」入口，文件列表 + Obsidian 式阅读器（思源宋体/黑体、15px）
- 链接悬停预览：阅读器中悬停笔记内链接 300ms 弹出预览浮层（Obsidian 双向链接同款体验），点击可跳转
- 原「笔记」导航更名为「工作台」

## [1.21.9] - 2026-08-12

### 新增
- 应用内笔记预览：批量处理完成报告的每个任务新增「预览」按钮，直接查看渲染后的 Markdown 笔记，无需跳转 Obsidian

## [1.21.8] - 2026-08-12

### 修复
- 抖音任务标题显示为链接而非视频标题：适配器返回标题后同步更新任务列表
- 抖音视频文件重复复制到音频目录：本地文件直接使用原始路径，不再复制到 audio_dir

## [1.21.7] - 2026-08-12

### 改进
- 关系图谱全面美化：渐变背景、辉光效果、SVG 滤镜、节点高亮、连线修复
- 修复 D3 力导向图连线不显示的问题（D3 会将 edge 的 source/target 从字符串改为对象引用）
- 节点数量上限从 15 提升到 20，边阈值从重叠 1 次提升到 2 次（减少噪音）

## [1.21.6] - 2026-08-12

### 改进
- 知识关联图谱改用 D3.js 力导向布局，节点有物理模拟动画，支持拖拽交互

## [1.21.5] - 2026-08-12

### 移除
- 移除知识关联面板的「对比」功能（上下文摘要太短，实际对比价值不大）

## [1.21.4] - 2026-08-12

### 改进
- 刷新按钮移至右上角顶栏，改名为「刷新」

## [1.21.3] - 2026-08-12

### 改进
- 欢迎卡片「重置」按钮始终显示（处理中时隐藏），方便随时刷新主界面状态

## [1.21.2] - 2026-08-12

### 新增
- 欢迎卡片底部新增「重置」按钮，任务完成后可一键清除流程面板状态，回到默认界面

## [1.21.1] - 2026-08-12

### 修复
- 欢迎区域「打开 Obsidian 库」按钮点击无反应：安全检查误拦截目录路径（要求文件扩展名白名单），现在目录路径单独处理

## [1.21.0] - 2026-08-12

### 改进
- 笔记链接格式从 Obsidian wiki-link (`[[名称]]`) 改为标准 Markdown 链接 (`[名称](路径.md)`)，兼容所有 Markdown 编辑器（Typora、VS Code 等），不再依赖 Obsidian
- 实体卡片的「来源内容」和「相关概念/术语」链接使用相对路径，可直接点击跳转
- 播客笔记正文中的实体链接在写入前自动转换为标准格式
- 向后兼容：反向链接索引同时支持新旧两种链接格式解析
- 导出为纯文本时正确去除标准 Markdown 链接语法

## [1.20.2] - 2026-08-07

### 修复
- 停止任务时误报"处理失败"：取消后 processPodcast 返回 null 会被当成失败处理，现在先检查取消状态，标记为「已停止」且不发失败通知
- 停止任务后流程面板继续转圈：取消时补充发送全部步骤的 stopped 状态
- 处理失败时 IPC 返回 success:false（原来误返回 success:true）

## [1.20.1] - 2026-08-07

### 改进
- Whisper 可执行文件路径从「高级设置」移到显眼位置，标记为必填（红色 *）
- placeholder 文案改为「必填：选择或自动检测 whisper 引擎文件」
- 新增引擎说明 + GitHub 下载链接提示

## [1.20.0] - 2026-08-07

### 新增
- Whisper 引擎路径自动检测：设置页新增「自动检测引擎」按钮（全盘递归搜索 faster-whisper-xxl.exe，找到自动填入并保存）
- 处理任务时若未配置 exe 路径，自动检测并写回配置，用户无需手动找文件

### 改进
- 设置保存后不再自动关闭界面，保留「✓ 保存成功」提示 3 秒

## [1.19.1] - 2026-08-07

### 改进
- 欢迎区升级：按时段动态问候（早上好/下午好/晚上好）
- 新增今日完成/累计处理统计
- 状态徽章加呼吸圆点（处理中/批量/暂停/待命不同颜色）
- 新增「打开 Obsidian 库」快捷按钮
- 精简欢迎区描述文案

## [1.19.0] - 2026-08-07

### 改进
- 重新设计右侧任务面板：Tab 栏与面板融合为整体卡片
- Tab 按钮改为 pill 式（选中高亮背景 + accent 色），去掉底部直线分隔
- 任务卡片 hover 提升（accent 微光背景 + 轻阴影）
- 面板间距、圆角、层次重新打磨

## [1.18.4] - 2026-08-07

### 修复
- 活跃任务/历史记录 Tab 栏底部 border 直线改为两端渐隐的渐变分隔线，圆角顶部不再有生硬直线

## [1.18.3] - 2026-08-07

### 修复
- 任务面板（活跃任务/历史记录）圆角边缘阴影直线问题：去掉 panel-shadow 的 1px outline 层和 backdrop-filter（Electron 渲染伪影）

## [1.18.2] - 2026-08-07

### 新增
- 视图切换动画：笔记/知识关联/搜索三个面板切换时淡入淡出 + 位移（AnimatePresence）
- 右侧任务面板动画：活跃任务/历史记录面板进入动画，任务卡片增删时淡入淡出 + 平滑布局

## [1.18.1] - 2026-08-07

### 修复
- 飞书测试连接结果改为结构化 code 返回，英文模式正确显示（原来显示中文）
- 补全缺失翻译 key：监听运行中/未启动、飞书已连接/未连接、状态栏、批量队列、任务面板等 80+ 条
- 设置页飞书测试结果显示框英文适配

### 改进
- 顶部栏新增可拖动空白区：搜索框限宽 420px，旁边留出整条拖动手柄区域，小窗口时也能舒适拖动窗口
- 关于页根据项目实际内容重写（功能特性、技术栈、双语）

## [1.18.0] - 2026-08-07

### 新增
- 全界面中英双语：设置对话框右侧全部参数区、工作区欢迎区、新任务输入区、拖动上传区、批量队列、搜索、知识关联、命令面板、关于页等全部翻译为英文
- 翻译策略改为「中文原文即 key」，en 模式查字典，zh 模式原样显示，杜绝遗漏
- 动态消息（模型扫描、测试连接、导出结果等）也支持双语

### 修复
- 修复 hooks 依赖警告，lint 0 warnings

## [1.17.0] - 2026-08-07

### 新增
- 语言切换功能：顶部栏新增中/英切换按钮（🌐 EN/中文）
- 支持界面语言在中文和 English 之间切换，选择持久化保存（localStorage）
- 翻译覆盖：顶部栏、侧边栏、输入框、步骤面板、任务面板、批量队列、设置对话框

## [1.16.1] - 2026-08-07

### 品牌更名
- 项目更名为 PodMuse（播客缪斯），GitHub 仓库迁移到 xuxuyouxiu/PodMuse
- 安装包、窗口标题、托盘、通知、侧边栏、关于页全部更新为 PodMuse
- 新增 userData 目录迁移，老用户升级配置不丢失

### 修复
- 流程面板新任务开始时重置步骤状态
- 飞书通知发送失败时记录错误日志
- 知识关联/搜索搜索框聚焦图标变色，去掉内层紫色框
- 抖音链接支持飞书消息自动处理

### UI
- 顶部栏去掉重复品牌区，版本号移入侧边栏底部

## [1.16.0] - 2026-08-01

### 新增
- 支持抖音链接：粘贴抖音视频/图文链接即可自动下载音频、转写、生成笔记（通过 yt-dlp，支持短链自动解析）

## [1.15.1] - 2026-08-01

### 性能优化
- 音频下载改用流式写入（createWriteStream），大文件不再撑爆内存
- 搜索笔记扫描结果加 mtime 缓存，连续搜索零磁盘 I/O
- 批量队列持久化改为 300ms debounce，减少 99% 磁盘写入
- loadState / loadConfig 加 mtime 缓存，避免重复读盘解析 JSON
- ProcessedMessageStore flush 后同步截断内存 Set，防止长时间运行内存膨胀
- AI API 调用添加 120s 独立超时，防止接口挂起导致任务无限等待

## [1.15.0] - 2026-07-31

### 修复
- shell:showInFolder 增加路径安全验证，复用 isSafeFilePath
- app:cleanTemp 仅允许清理应用默认临时目录，防止 audio_dir 被篡改后误删
- Whisper 子进程取消时 Windows 上用 taskkill /F /T 杀进程树，避免僵尸进程
- dedup-store Set 上限 10000，超限淘汰最早一半，防止内存无限增长
- App.tsx 初始加载配置/任务失败时显示 toast 而非静默吞错
- 设置对话框窄屏适配：侧边栏可收缩，内容区正确滚动

### 新增
- 飞书配置「测试连接」按钮，同时验证 App ID/Secret 和 Chat ID
- 保存配置后自动重连飞书并显示 toast 提示
- Whisper 模型扫描自动检测 Faster-Whisper-XXL 安装路径
- 单实例限制：双击打开软件时聚焦已有窗口
- GitHub Release workflow，推送 tag 自动上传安装包
- 基础可访问性改进：toast aria-live、对话框 role=dialog、按钮 aria-label

### 改进
- TabApi/TabExport 内联样式统一为 CSS class（.settings-provider-grid 等）
- TabWhisper 高级设置按钮改用 .settings-link-button class
- 设置页面所有外部链接改为系统浏览器打开
- 导出菜单靠近窗口边缘时自动调整弹出方向
- 搜索面板和知识关联搜索框样式统一为主页风格

### 重构
- Prettier 格式化全部源文件（86 个文件）
- ESLint warnings 清理到 0
- getResourcePath 移除重复的 dev/prod 分支
- .gitignore 补充 .vscode/、coverage/、*.tsbuildinfo 等

### 测试
- 新增 config.test.ts（39 个用例）
- 新增 backlinks.test.ts（31 个用例）
- 新增 notion-converter.test.ts（46 个用例）
- 测试总数从 54 个增加到 170 个

## [1.14.6] - 2026-07-20

### 修复
- 单实例限制：双击打开软件时不再创建第二个窗口，而是聚焦已有的窗口

## [1.14.5] - 2026-07-20

### 改进
- 新增 GitHub Release workflow，推送 tag 自动构建并上传安装包到 Releases

## [1.14.4] - 2026-07-20

### 修复
- Whisper 模型扫描误判修复：普通目录必须包含模型文件（model.bin / config.json 等）才算已下载，不再因目录名叫 "base" 就误报

## [1.14.3] - 2026-07-20

### 改进
- Whisper 模型扫描自动检测 Faster-Whisper-XXL 安装路径（扫描常见目录和 PATH），无需手动配置 exe 路径即可识别已下载模型

## [1.14.2] - 2026-07-20

### 修复
- 加载配置时自动清理旧版 example config 遗留的占位值（以"你的"开头的字段清为空字符串），重装后不再看到假数据

## [1.14.1] - 2026-07-20

### 修复
- 飞书配置输入框增加 placeholder 提示（App ID / App Secret / Chat ID）
- 安装版首次启动不再预填飞书/API 占位值，example config 敏感字段改为空字符串
- Whisper 模型扫描增加 Hugging Face 缓存目录（`~/.cache/huggingface/hub`）和 exe 旁 `models/` 目录，支持 `models--Systran--faster-whisper-*` 格式匹配

## [1.14.0] - 2026-07-20

### 新增
- 飞书配置「测试连接」按钮：填写 App ID/Secret 后可一键验证凭据是否有效，无需保存后再看日志
- 保存配置后自动重连飞书并显示 toast 提示（连接成功/失败/异常），不再静默吞掉错误

## [1.13.2] - 2026-07-20

### 修复
- 安装版配置丢失：`electron-builder.yml` 的 `extraFiles` 错误打包了 `portable` 标记文件，导致安装版被当作便携版运行，配置存到安装目录（重装即丢失）。移除 `portable` 后安装版正确使用 `%APPDATA%/播客笔记助手/` 存储配置
- 便携版构建脚本：`scripts/build-portable.ps1` 重写为完整构建脚本（备份 data → 构建 → 复制 portable 标记 → 恢复 data），新增 `npm run build:portable` 命令

## [1.13.1] - 2026-07-19

### 修复
- 基于代码排查的 7 个潜在改进（全部 <1 天工作量、真实代码证据）
- 安全：`SearchPanel.tsx` 标题 `dangerouslySetInnerHTML` XSS 漏洞 — `search.ts` 新增 `highlightTitle()`，标题先 `escapeHtml` 再用 `<mark>` 包裹关键词，与 excerpt 路径一致
- 健壮性：`feishu-client.ts` 的 `feishuApi` 加 `AbortSignal.timeout(15000)`，避免飞书 API 网络挂起时 `MessagePoller.tick()` 永久阻塞、`scanning` 标志永不清除
- 健壮性：`batch-queue.ts` 的 `resume()` 两处 `processNext().catch(() => {})` 改为通过 `callbacks.sendLog` + `console.error` 上报，避免未捕获异常被静默吞掉导致队列"假停止"
- 用户体验：`App.tsx` 的 `handleBatchRetryAllFailed` 改 async + `for...of await`，先等待每个 `batchRetry` IPC 完成再 `batchStart`，否则 start 时队列还是 `failed` 不会启动
- 用户体验：`config.ts` 的 `saveState` 不再静默吞错，磁盘满/权限错时通过新 `toast` IPC 通知前端显示错误（preload 暴露 `onToast`、env.d.ts 类型、App.tsx useEffect 监听）
- 数据完整性：`shared/utils.ts` 的 `cleanTitle` 在 `cleaned` 为空（纯日期/期数标题被清空）时不再 fallback 原始标题，改为生成 `未命名播客_${YYYYMMDDHHmmss}` 唯一名，避免多期同日期笔记文件互相覆盖
- 用户体验：`platforms/xiaoyuzhou.ts` 的 `extractAudio` 签名加 `signal?: AbortSignal` 参数（与 B 站适配器一致），传给 `fetch`，用户点取消时能中断小宇宙页面抓取

## [1.13.0] - 2026-07-19

### 新增
- 导出更多平台（ICE 6.0）：完成笔记后手动导出到 Markdown / Logseq / Notion，单任务触发，不侵入批量流程
  - 新增 `exporter.ts`：通用复制核心（`copyNoteToDir` + `stripWikiLinks` + `getNotePathByTaskId` + `exportNote` 总入口），文件名冲突自动追加时间戳后缀（YYYYMMDDHHmmss），`NodeJS.ErrnoException` 按 code 映射中文错误（ENOENT/EACCES/ENOSPC/ENAMETOOLONG/EROFS）
  - 新增 `notion-converter.ts`：无 Notion SDK 直接 `fetch` 调 REST API（`Notion-Version: 2022-06-28`），极简按行 markdown 解析（标题/段落/列表/引用/todo/code/divider），frontmatter → Notion properties（title/show/episode/host/guest/platform→rich_text；date→date；category/platform→select；tags→multi_select），上传前 GET database schema 校验列名，重复检测（query by title），100 blocks 上限截断
  - 新增 4 个 IPC：`export:toMarkdown` / `export:toLogseq` / `export:toNotion` / `export:notion:testConnection`，preload 桥接 + env.d.ts 类型声明
  - 新增 `ExportMenu` 组件：RecentTask 完成卡片「导出」下拉菜单，未配置项禁用 + 悬停提示，导出中按钮 spinner + disabled
  - 新增 `TabExport` 设置 tab：Logseq 目录配置（DirField）+ Notion token（password 输入）/ database_id 配置 + 测试连接按钮 + 测试结果显示 + 集成说明
  - 新增 `ExportConfig` / `NotionConfig` 接口扩展 `PodcastConfig.export` 字段，DEFAULTS 默认值 + `validateConfigInput` 嵌套对象校验（logseq_dir 路径安全 + notion token/database_id 类型校验）
- 新增文档：`docs/用户故事-导出更多平台.md`（4 个 US：Markdown P0 / Logseq P1 / Notion P1 / 配置 UI P0）、`docs/PRD-导出更多平台.md`（完整类型定义 + 算法说明 + 数据流 + IPC 合约 + UI 设计 + 性能要求 + 风险降级）

### 改进
- App.tsx 的 toast 状态从 `string | null` 扩展为 `{ message: string; type: 'success' | 'error' } | null`，新增 `showToast(message, type?)` 函数，渲染根据类型显示 ✓/✗ 图标 + 对应颜色边框，支撑导出成功/失败不同 UX
- `RecentTasksPanel` 接收 `logseqDir` / `notionConfigured` / `onToast` props，仅在 status=completed 且 task.filename 存在时渲染 ExportMenu
- `SettingsDialog` 新增第 6 个 tab「导出」（图标 Download），`initialConfig` 补充 export 字段默认值（向后兼容旧 config）

## [1.12.0] - 2026-07-19

### 新增
- 增强搜索（ICE 10.5）：独立搜索视图，支持全文搜索 + 分面过滤 + 字段加权评分 + 高亮摘录
  - 后端 `search.ts`：无外部库实现，字符级 bigram 中文分词（`中文搜索` → `中文`/`文搜`/`搜索` + 完整片段），字段加权评分（标题 5x / 标签 3x / 内容 1x，单 token 内容命中上限 5），高亮摘录构建器（±80 字符上下文，HTML 转义 + `<mark>` 包裹防 XSS）
  - 分面过滤：分类/标签/节目/日期范围/实体引用五维筛选，二次过滤（分面根据当前过滤器动态更新，避免 0 结果组合），mtime 失效缓存（`facetCache` + `facetCacheDir`）
  - 独立 `SearchPanel` 视图：搜索框 + 排序（相关度/日期）+ 分页 + 左侧分面侧栏 + 右侧结果卡片，`<mark>` 高亮通过 `dangerouslySetInnerHTML` 渲染（摘录内容已 HTML 转义），分类徽章 `CATEGORY_COLORS` 映射
  - 实体过滤：`entityRefs` 参数支持按实体引用筛选笔记，复用 `ENTITY_DIRS` 常量
  - 新增 IPC `search:enhanced` + `search:facets`，preload `searchEnhanced` + `searchFacets` 桥接
  - `WorkspaceSidebar` 新增 `search` 视图入口（Search 图标）
- 新增文档：`docs/用户故事-增强搜索.md`（4 个 US：后端 P0 / 分面 P1 / UI P1 / 实体 P2）、`docs/PRD-增强搜索.md`（完整类型定义 + 算法说明 + 文件变更清单 + IPC 合约 + UI 设计 + 性能要求）

### 改进
- `backlinks.ts`：`FrontmatterMeta` 接口扩展 `title?`/`host?`/`guest?`/`platform?` 字段，`parseFrontmatter` 解析对应键，导出 `ENTITY_DIRS` 常量 + `parseFrontmatter` 函数供 search.ts 复用

## [1.11.0] - 2026-07-19

### 新增
- 内容标签与聚类：知识关联面板新增"标签"顶级视图，支持标签云浏览、分类聚类、标签详情、相关笔记推荐（ICE 14.0）
  - 标签云：按频率动态映射字体大小（12-22px），点击标签查看详情
  - 分类聚类：科技商业/每日资讯/社会心理/生活文化 4 类卡片，点击查看该分类下所有标签和笔记
  - 标签详情：展示标签下所有播客笔记列表（标题/日期/分类/节目）
  - 相关笔记推荐：Jaccard 相似度算法，基于标签重合度推荐 Top 3 相关笔记
  - 后端 `buildTagIndex` 函数：扫描 Obsidian 目录所有笔记 frontmatter 的 tags 字段，构建 `TagEntry[]` 索引，按数量降序排序
  - 新增 IPC `tags:getIndex` + preload `getTagIndex` 桥接
- 需求优先级排序更新：删除"转写结果可编辑"需求（用户判断手动修正逐字稿太耗时、体验鸡肋）
- 新增文档：`docs/用户故事-内容标签与聚类.md`、`docs/PRD-内容标签与聚类.md`

### 改进
- `BacklinkPanel` 组件支持实体/标签双顶级视图切换
- `FrontmatterMeta` 接口扩展 `tags?: string[]` 字段，`parseFrontmatter` 支持 `[a, b, c]` 数组解析

## [1.10.21] - 2026-06-23

### 新增
- 长文本分段处理：播客逐字稿超过 30000 字符时自动分最多 3 段处理（每段 22000 字符，500 字符重叠），分段进度实时显示，合并补充内容到完整笔记
- AI 质量评估评分：笔记生成后本地计算 4 维评分（内容覆盖率 40% + 实体完整性 25% + Wiki-link 覆盖率 20% + 格式合规 15%），低于 60 分标红警告建议复核
- Prompt 模板可配置：应用启动时自动在 `{userData}/prompts/` 导出内置模板参考文件（`.default.md`），用户可创建 `note-generation.md` 或 `transcript-correction.md` 覆盖内置 prompt
- 实体关系图谱：知识关联面板新增"图谱"按钮，SVG 圆形布局展示实体共现关系，中心节点为当前选中实体，周围环绕最多 15 个共现实体，悬停高亮关联边和节点，点击节点跳转该实体时间线
- 实体卡片"近期提及"自动更新：每次处理播客后，自动更新被提及实体卡片的"# 近期提及"段落，保留最近 3 条记录，按播客日期倒序排列，自动去重

### 改进
- 批量处理面板 UI 优化：任务卡片布局改进，队列状态展示更清晰
- 活跃任务面板交互优化：重新处理按钮、任务状态展示改进
- 飞书监控器批量模式兼容：批量处理时自动切换调度模式

## [1.10.20] - 2026-06-22

### 新增
- 实体卡片自动更新摘要：处理播客后自动更新实体卡片的"# 近期提及"段落（跨期知识积累 US-004）
- 反向链接面板上下文对比功能：支持实体在不同播客中的上下文句子级对比（跨期知识积累 US-002）

### 改进
- 实体卡片写入支持传入播客标题、日期、期数等元数据

## [1.10.19] - 2026-06-22

### 新增
- 实体关系图谱：知识关联面板新增 SVG 图谱视图（跨期知识积累 US-003），展示实体共现关系网络

## [1.10.0] - 2026-06-20

### 新增
- 知识关联面板：扫描 Obsidian 目录构建实体→播客反向链接索引，展示哪些节目提到了同一实体
- 实体分类展示：人物/项目/概念/术语四类实体用不同图标和颜色区分，按提及次数排序
- 播客元数据展示：反向链接列表显示每期节目的标题、日期、分类和播客名
- 点击跳转：点击播客条目在 Obsidian 中打开笔记，右键可在文件管理器中定位
- 共现实体推荐：展开某实体时自动计算并展示与之共同出现的其他实体
- 侧边栏视图切换：新增"知识关联"导航入口，工作台与知识关联双视图切换
- 任务概览位置调整：侧边栏任务概览移至设置按钮上方

## [1.9.0] - 2026-06-19

### 新增
- 喜马拉雅适配器：支持 ximalaya.com 单集音频，通过移动端 baseInfo API 获取标题、音频流、主播名，支持付费内容检测
- Apple Podcasts 适配器：支持 podcasts.apple.com 单集链接，通过 iTunes Lookup API 获取 RSS → 解析 XML → 匹配单集
- 元数据管道：各平台适配器提取的元数据（UP主、频道名、内容简介）注入 AI 提示词上下文，提升实体卡片质量
- 输入框平台标识：粘贴链接后实时识别平台，显示对应图标和平台名 badge，不支持的平台显示提示
- 设置页"支持平台"Tab：展示所有已支持平台列表及状态，yt-dlp 检测状态和版本号，可一键重新检测

### 改进
- B 站适配器：新增 UP 主名称和视频描述元数据
- YouTube 适配器：通过 oEmbed API 获取频道名称，并行获取标题和频道信息
- 小宇宙 fetchOgTitle：支持 AbortSignal 超时控制

## [1.8.3] - 2026-06-18

### 新增
- yt-dlp 自动下载：首次使用 YouTube 链接时若未检测到 yt-dlp，自动从 GitHub Releases 下载最新版，用户无需手动安装
- 下载进度实时显示在步骤面板（百分比 + 已下载/总大小）

### 改进
- 顶栏搜索框 UI 统一为与 URL 输入框一致的风格（focus glow、图标变色、清除按钮）
- 输入框与搜索框均消除浏览器默认 focus outline，修复紫色双边框问题

## [1.8.2] - 2026-06-18

### 修复
- YouTube 适配器缺少标题：通过 og:title 获取视频标题，清理 `- YouTube` 后缀，笔记文件名不再降级为"未命名播客"

## [1.8.1] - 2026-06-18

### 修复
- B 站链接处理失败：yt-dlp 被 B 站 412 反爬拦截，改为直接调用 B 站 API 获取 DASH 音频流，不再依赖 yt-dlp 处理 B 站链接
- B 站适配器缺少标题：通过 B 站 view API 获取视频标题，文件名不再降级为 BV 号
- 清理 B 站标题常见后缀（如 `_哔哩哔哩_bilibili`）
- 音频下载支持自定义请求头（B 站 CDN 需要 Referer），新增 m4s 格式支持
- yt-dlp 搜索路径新增用户级工具目录（`~/tools`、`~/.local/bin`），构建时自动打包 yt-dlp.exe

## [1.8.0] - 2026-06-18

### 新增
- 多平台支持：新增 B 站、YouTube、直接音视频 URL 三种平台适配器，粘贴链接即可自动识别并处理
- yt-dlp 集成：B 站和 YouTube 视频通过 yt-dlp 提取音频，支持自动检测、版本检查和安装引导
- YouTube 字幕优先策略：有 CC 字幕时直接使用字幕文本跳过 Whisper 转写，精度更高速度更快
- 平台适配器架构（PlatformAdapter）：统一的接口定义 + 注册表路由，新增平台只需实现适配器并注册
- 直接音视频 URL 支持：任意 `.mp3/.mp4/.m4a` 等直链可直接粘贴处理
- 飞书消息多平台解析：飞书群中分享 B 站、YouTube 等链接也能自动触发处理

### 重构
- `podcast.ts`：URL 验证从硬编码小宇宙正则改为平台注册表路由，音频提取逻辑迁移到各平台适配器
- `message-parser.ts`：飞书消息 URL 匹配从单平台扩展为多平台正则列表
- `index.ts`：episode ID 提取使用平台适配器的 `getDedupKey()` 方法，支持所有平台去重

### 修复
- UI 输入框提示文案更新为多平台描述

## [1.7.14] - 2026-06-10

### 修复
- 拖入本地文件时不复用已有转写缓存：原逻辑仅在 `force=true`（重新处理）时检查 `.transcript.json` 缓存，导致每次拖入同一文件都会重新跑 Whisper 转写。现在改为默认检查缓存，`force=true` 时强制跳过缓存重新转写
- 任务"重新处理"时复用缓存：`handleTaskResume` 改为 `force=false`，恢复任务时优先使用已有转写结果

## [1.7.13] - 2026-06-09

### 修复
- 活跃任务面板"重新处理"按钮显示为纯文本：原来使用了不存在的 CSS 类名 `recent-task-action`，改为 `recent-task-primary`，现在有正确的渐变背景和圆角样式
- "开始处理"和"浏览文件"按钮图标与文字上下堆叠：为 `.url-input-submit` 和 `.file-drop-browse` 添加 `display: inline-flex; align-items: center; gap: 6px`，图标和文字改为水平排列

## [1.7.12] - 2026-06-09

### 修复
- 暂停后任务从活跃列表跑到历史列表：`stopRecentTask` 原来会把停止的任务从 activeTasks 移到 recentTasks，导致任务"消失"到历史中。现在停止的任务保留在活跃列表，方便重新处理
- 流程面板暂停时大 Pause 图标位置不合理：移除了多余的大图标，仅保留文字提示"处理已停止"

### 优化
- 去掉流程面板底部的"继续处理"按钮，停止和重新处理统一在右侧活跃任务面板操作
- 活跃任务面板为已停止/失败的任务增加"重新处理"按钮

## [1.7.11] - 2026-06-09

### 优化
- 去掉流程面板中的"停止处理"按钮，统一由右侧活跃任务栏提供停止功能，避免重复入口

## [1.7.10] - 2026-06-09

### 修复
- 正文 wiki-link 在访谈/非新闻类播客中缺失：之前仅在核心原则中声明了内联链接要求，但各模块定义中缺少提醒，导致 AI 在访谈类内容中经常忽略。现在在模块 A（内容概览）、B（核心观点）、C（事件详情）、D（对话还原）的定义和格式示例中都加入了 [[名称]] 包裹要求，并将核心原则 #4 升级为"所有模块必须遵守"的强制规则，附带正确/错误示例对比

## [1.7.9] - 2026-06-09

### 变更
- 去掉手动内容类型选择，改为 AI 自动识别：不再需要用户手动选择"新闻资讯/长文章/教程/默认"，AI 会根据逐字稿内容自动判断类型并选择最合适的模块组合和信息密度。界面上的内容类型选择器和设置页中的默认内容类型选项均已移除

## [1.7.8] - 2026-06-09

### 修复
- 处理完成后仍然卡住不动：v1.7.7 虽然给每个请求加了超时，但概念和术语的定义仍是逐个串行获取（10+ 个概念 × 3 个数据源 × 超时 = 仍可能等待数分钟）。现在改为双重并行优化：
  1. **所有实体定义并行获取**：用 `Promise.allSettled` 同时发起所有概念和术语的定义查找，并设 30 秒全局超时兜底
  2. **单个定义的多源并行竞速**：`fetchConceptDefinition` 内部中文维基、英文维基、AI 三个源同时请求，用 `Promise.any` 取第一个成功结果，维基百科不通时不再白等 16 秒

## [1.7.7] - 2026-06-09

### 修复
- 处理完成后卡住不动：`fetchConceptDefinition` 调用维基百科 API 时没有超时限制，国内访问维基百科经常超时导致长时间挂起。现在每个网络请求加上 8 秒超时（维基百科）和 15 秒超时（AI），防止卡死
- 实体卡片生成过程无反馈：概念定义查找过程中增加进度日志（如"正在查找「XX」的定义（3/8）..."），方便观察进度

## [1.7.6] - 2026-06-09

### 修复
- 自动补全的概念卡片显示"本期未提及"：正文中 wiki-link 自动创建的概念卡片解释为空，模板默认显示"（本期未提及）"。现在写入概念卡片前会检查解释是否为空，为空时自动从维基百科（中文优先、英文备选）或 AI 获取真实定义填入
- 模板空值占位文本从"（本期未提及）"改为"（暂无详细信息）"，更准确

## [1.7.5] - 2026-06-09

### 新增
- 正文 wiki-link 自动补全卡片：正文中所有 `[[名称]]` 链接如果没有对应的实体卡片（人物/项目/概念/术语），自动创建一个概念卡片作为落地页，确保 Obsidian 中点击每个链接都能看到内容，而不是空白页

## [1.7.4] - 2026-06-09

### 修复
- 正文中人物名和公司名没有 wiki-link：事件详情中提到"黄仁勋"、"英伟达"、"Krafton"等时是纯文本，只有底部关联索引才有链接。现在要求正文中首次出现的人物名、公司/品牌名、项目名、术语名都用 [[名称]] 格式包裹，并给出具体示例

## [1.7.3] - 2026-06-09

### 修复
- v1.7.2 提示词约束过多导致 AI 输出内容严重缺失：新闻资讯类播客 16 个事件只生成了 3 个，大量内容（NBA、特朗普、黄仁勋等）被丢弃。根本原因是核心原则中叠加了太多硬性规则（严禁重复、实体索引严格对应卡片等），AI 在遵守规则时变得过度保守。现在精简约束，将「内容完整性」提升为最高优先级原则，明确要求"必须覆盖逐字稿中所有事件和话题，一个都不能少"
- AI 输出开头出现"好的，请查收"等对话文本：核心原则增加"不要添加任何开场白或结尾语"的明确要求

### 优化
- 正文术语链接：在正文首次提及术语时用 [[术语名]] 包裹，保留此功能但措辞更柔和
- 实体分类引导：保留人名/公司/概念的分类提示，但从硬性规则改为柔性建议

## [1.7.2] - 2026-06-09

### 修复
- 正文中术语无法直接跳转解释：术语词典中的术语在正文（事件详情、核心观点等章节）首次提及时未用 `[[术语名]]` 包裹，读者需手动翻到底部查看。现在要求 AI 在正文首次提及术语时使用 wiki-link 格式
- 关联实体索引条目缺少对应卡片：索引中列出的人物/项目/概念没有全部生成对应的 CARD 卡片（如英伟达、赛百味等只列了名但没有卡片），导致 wiki-link 指向空页面。现在要求索引与卡片严格对应，没有卡片的条目不列入索引
- 关联概念中混入人名：AI 将人名（如成兴、张驰）错误归类到「关联概念」而非「关联人物」。新增分类互斥规则：人名只能放在关联人物，公司/品牌只能放在关联项目，抽象概念只能放在关联概念
- 同一实体重复生成多种卡片：如「全服务航空公司」同时出现在 CARD-CONCEPT 和 CARD-TERM 中。新增规则：同一实体只允许出现在一种卡片类型中
- 正文事件章节内容重复：AI 对同一新闻生成了两个几乎相同的章节（如 OpenAI 转型超级应用出现两次）。新增严禁重复规则：同一事件只生成一个章节，内容相似的事件合并为一个

## [1.7.1] - 2026-06-09

### 修复
- 重处理时未兼容 Whisper 旧版 `.txt` 转写文件：缓存功能开发前处理的音频只有 Whisper 输出的 `.txt` 文件，没有 `.transcript.json`，导致缓存始终未命中仍会重新转写。现在回退查找同目录的 `.txt` 文件，有效则复用并自动迁移为 JSON 缓存格式

## [1.7.0] - 2026-06-09

### 新增
- 转写结果缓存机制：Whisper 语音转文字结果自动缓存为 `.transcript.json` 文件，重处理（删除笔记后重新处理同一链接）时自动检测并复用有效缓存，跳过耗时的 Whisper 转写步骤，大幅提升重处理速度
- 缓存有效性校验：重处理时校验缓存文件完整性（文件大小 ≥ 100 字节）、JSON 格式合法性、`text` 字段存在性及类型、转写文本长度 ≥ 50 字符，任一校验不通过则自动重新转写
- 损坏缓存自动清理：校验失败的缓存文件自动删除，避免残留无效文件
- 完整日志记录：每次重处理记录文件检查结果、复用情况或重新转写触发原因，便于问题排查

### 优化
- AI 提示词模块化重构：从固定模板改为 8 个可选内容模块（A-H），AI 根据内容类型自主选择合适模块组合，不需要的模块直接省略不显示
- 内容类型引导增强：新闻/文章/教程/默认四种类型各有专属引导策略，明确推荐和不推荐的模块组合

## [1.6.9] - 2026-06-08

### 修复
- 新闻资讯类型未自动归类到「每日资讯」：AI 提示词未强制绑定分类，导致 AI 可能选择错误类别。现在 `news` 内容类型强制 `category: 每日资讯`
- 新闻资讯类型包含不适用的「关键对话还原」章节：模板未区分内容类型，所有类型共用同一结构。现在新闻类型自动跳过该章节
- 新闻资讯模板信息密度过低：原模板仅一句"保持简洁"，导致关键信息丢失。新模板要求每条新闻包含事件摘要、影响分析、关键数据、相关方立场、背景信息等完整要素

## [1.6.8] - 2026-06-07

### 修复
- 修复「加载模型」按钮使用脱敏值导致 API 调用失败：`loadSafeConfig()` 后端脱敏将 API Key 变为 `****xxxx`，UI state 持有该脱敏值并传递给 `ai:fetchModels`，导致模型列表获取失败。现移除后端脱敏，前端 `type="password"` 已提供足够的视觉保护
- `ai:fetchModels` 增加脱敏值检测：即使收到 `****` 开头的脱敏 Key，也会自动从配置文件读取真实值进行 API 调用

## [1.6.7] - 2026-06-07

### 修复
- 彻底移除 safeStorage (DPAPI) 加密机制，凭据改为明文存储。Windows DPAPI 熵值绑定 Electron 二进制身份，每次重建后旧加密值不可解密，导致重启后配置丢失

## [1.6.6] - 2026-06-07

### 修复
- 设置保存后 API Key 和飞书 Secret 丢失：`config:save` 处理器在合并配置时，前端传回的脱敏值（`****xxxx`）覆盖了后端真实解密值。现在检测到脱敏值时自动保留已有的真实凭据
- 彻底移除 safeStorage (DPAPI) 加密机制：Windows DPAPI 的熵值随 Electron 构建版本变化，导致加密的凭据在应用重建后永远无法解密。改为明文存储凭据，配置文件位于用户数据目录已有操作系统权限保护。保留一次性迁移逻辑，尝试解密旧的 `_enc` 字段

## [1.6.5] - 2026-06-06

### 新增
- 便携模式支持：win-unpacked 目录下存在 `portable` 标记文件时，配置和状态数据存储在 `exe同级/data/` 而非 `%APPDATA%`，实现真正的便携版

### 修复
- 设置保存后飞书不自动重连：`handleSaveConfig` 保存配置后现在自动调用 `startFeishu()` 使用新凭据重新连接监听器

## [1.6.4] - 2026-06-06

### 修复
- 凭据解密失败时静默错误：`decryptField` 解密失败时将 base64 密文当作"原文"返回，导致 API 调用使用乱码凭据。现在解密失败会抛出异常并由上层正确处理
- `decryptConfigFields` 新增逐字段错误处理：单个字段解密失败不再影响其他字段，失败的字段会重置为空值并记录到 `_decryptionFailedFields`
- `feishu:start` 启动时检测解密失败字段并通过日志通知用户重新输入凭据

## [1.6.3] - 2026-06-06

### 修复
- 飞书监听器启动时无反馈：`MessagePoller.start()` 在 `ensureToken()` 失败时静默返回，用户无法知晓原因。现在会明确记录失败原因并通知 UI
- 飞书 API 网络异常未捕获：`feishuApi()` 的 `fetch()` 调用缺少 try/catch，网络错误直接抛出未处理异常。现已捕获并返回友好错误码
- 飞书鉴权失败缺少凭据检查：App ID 或 App Secret 为空时仍尝试请求，现提前校验并给出配置提示
- `feishu:start` IPC 处理器缺少 try/catch：异常直接传播为未捕获的 Promise 拒绝，现已包装异常处理并返回安全默认状态

## [1.6.2] - 2026-06-06

### 修复
- Hero 区域长标题截断内容类型选择器：为 `.workspace-hero__meta` 添加 `min-width: 0; flex: 1` 允许标题收缩省略，为 `.workspace-hero__type-selector` 添加 `flex-shrink: 0` 确保选择器始终可见

## [1.6.1] - 2026-06-06

### 修复
- 搜索框下拉结果被主内容区遮挡：`backdrop-filter` 创建独立层叠上下文导致下拉框 z-index 失效，为 `.topbar-root` 添加 `position: relative; z-index: 100` 确保顶栏始终在最上层

## [1.6.0] - 2026-06-06

### 新增
- Command Palette 命令面板（Ctrl+Shift+P）：支持模糊搜索 8 个内置命令（切换主题、打开设置、搜索笔记等）
- Skeleton 骨架屏：配置加载期间展示全屏骨架动画，替代空白闪烁
- 侧边栏任务概览：实时显示进行中、排队中、今日完成三项统计
- 首次使用引导：StepPanel 空状态新增 3 步快速上手提示（粘贴链接 → 自动转写 → 推送飞书）

### 优化
- 设计审计 P1 代码质量提升：
  - Header、ConfirmDialog、StatusBar、StepPanel、AboutDialog 共 5 个组件的 inline style 全面迁移至 globals.css
  - 新增 `topbar-*`、`confirm-dialog-*`、`statusbar-pill*`、`step-node-*`、`about-dialog-*` 等语义化 CSS 类
  - 3 个组件中的重复 `@keyframes` 统一到 globals.css（fadeIn、modalSlide、step-node-pulse、step-connector-flow）
  - 建立模块化字号变量体系（`--fs-xs` 到 `--fs-2xl`）
- 设计审计 P2 体验提升：
  - SettingsDialog tab 导航 emoji（🔗📝🎙🛠）替换为 lucide-react 图标组件（Link、FileText、Mic、Wrench）
  - TabApi 供应商 emoji（🐋🤖🌙🧠☁️💡🐚⚙️）替换为 lucide-react 图标（Fish、Bot、Moon、Cpu 等）
  - TabWhisper emoji（🔗⚠✖⬇）替换为 lucide-react 图标（ExternalLink、AlertTriangle 等）
  - 窗口控制按钮添加 hover 状态：最小化/最大化悬停高亮，关闭按钮悬停变红
  - 全局 focus-visible 无障碍样式：键盘导航焦点指示器，输入框聚焦边框高亮
- 设计审计 P3 布局与交互优化：
  - 右侧面板从上下堆叠改为 Tab 切换（活跃任务 / 历史记录），节省纵向空间
  - 侧边栏新增任务统计卡片，带语义色图标（紫色/黄色/绿色）

### 修复
- 搜索框 `.is-open` 状态移除紫色边框和辉光，改为低调的 border-light 样式
- 右侧面板 Tab 标签添加 `white-space: nowrap`，防止"历史记录"文字折行

## [1.5.0] - 2026-06-06

### 优化
- 设计系统色彩体系重构（P0 审计修复）：
  - `:root` 改为浅色主题基准值，`body[data-theme='dark']` 仅做增量覆盖，消除 25 行重复变量定义
  - 新增语义色变量（`--overlay-bg`、`--accent-rgb`、`--success-rgb`、`--warning-rgb`、`--error-rgb`、`--menu-bg` 等）
  - 清除 8 个组件中的硬编码色值（`#4caf50`、`#1a1a2e`、`rgba(108,92,231,...)`、`rgba(0,0,0,0.6)` 等），统一使用 CSS 变量
  - 移除未使用的 shadcn `@layer base` 块（约 80 行），消除 `--accent` / `--border` 变量命名冲突
  - `.content-type-menu` 和 `select option` 背景色改用 CSS 变量，深浅主题自动适配

## [1.4.6] - 2026-06-05

### 优化
- 三阶段代码质量全面提升（安全/架构/工程化）：62 文件重构，+2796/-2421 行
  - 安全加固：IPC 输入验证、路径遍历防护、API Key 加密存储
  - 架构重构：主进程模块化拆分（IPC/配置/搜索/消息轮询独立模块）
  - 工程化改进：Vitest 测试框架接入（54 测试用例）、GitHub Actions CI 流水线
- ESLint 零警告：清理 25 个 lint 警告（未使用导入、死代码、Hook 依赖等）
- CI 类型错误修复：修正 `AboutDialog.tsx` 未使用的 `Github` 导入、`SettingsDialog/TabApi` 类型过窄问题
- CI lint 错误修复：修正 JSX 中文引号转义、移除 `motion`/`AnimatePresence` 等未使用导入

## [1.4.5] - 2026-06-02

### 修复
- 修复搜索栏被内容类型菜单遮挡问题：提高搜索结果z-index到10000
- 修复内容类型更改后无法重新处理相同URL的问题：当contentType改变时允许重新处理

## [1.4.4] - 2026-06-02

### 修复
- 修复链接点击行为：使用shell.openExternal在用户默认浏览器中打开GitHub链接
- 修复关于对话框中GitHub仓库链接和Issue链接指向错误仓库的问题，改为指向正确的项目仓库

## [1.4.2] - 2026-06-02

### 修复
- 修复浅色模式下拉菜单背景为黑色的问题：浅色模式使用白色背景#ffffff

## [1.4.1] - 2026-06-02

### 优化
- 下拉菜单背景改为不透明：使用深色背景#1a1a2e，增强阴影效果，提升可读性

## [1.4.0] - 2026-06-02

### 优化
- 调整下拉菜单位置：改用left定位，菜单左边缘对齐按钮左边缘，避免超出左侧侧边栏

## [1.3.9] - 2026-06-02

### 修复
- 修复下拉菜单被父容器裁剪问题：使用Portal将菜单渲染到document.body，采用fixed定位
- 提升菜单z-index到9999，确保始终显示在最顶层
- 添加滚动事件监听，菜单跟随按钮位置更新

## [1.3.8] - 2026-06-02

### 优化
- 内容类型选择器移至"欢迎回来"卡片：更直观的位置，与当前节目信息并列显示
- 修复下拉菜单显示不全问题：使用AnimatePresence动画和正确的z-index层级

## [1.3.7] - 2026-06-02

### 新增
- 主界面快速切换内容类型：在URL输入框旁添加内容类型选择器
- 支持4种类型快速切换：默认、新闻资讯、长文章/演讲、教程/课程
- 记住上次选择：内容类型选择自动保存到本地存储

### 优化
- 无需进入设置即可切换内容类型，提升使用效率

## [1.0.36] - 2026-06-02

### 新增
- 多模板系统：支持为不同类型内容提供不同模板
- 新增长文章/演讲专用模板（Podcast_Template_Detailed.md），保留具体例子和详细论证
- 新增新闻资讯专用模板（Podcast_Template_News.md），结构简洁重点突出
- AI提示词动态生成：根据内容类型生成不同的提示词
- 设置界面添加内容类型选择：默认、新闻资讯、长文章/演讲、教程/课程

### 优化
- 修复代码警告：移除未使用的变量和函数
- 模板加载逻辑：优先使用指定类型的模板，不存在时回退到默认模板

## [1.0.35] - 2026-05-31

### 改进
- 笔记模板添加「本期主要内容」部分
- 修复实体卡片解析逻辑，支持解析多个同类 CARD 块

## [1.0.34] - 2026-05-31

### 改进
- 优化笔记文件名：移除日期、期数等标识，只保留主要标题
- 将 cleanTitle 函数提取到共享工具文件，统一标题清理逻辑

## [1.0.33] - 2026-05-31

### 修复
- 修复设置界面下拉框选项在深色模式下不可见：添加colorScheme:dark内联样式和option元素直接颜色值

## [1.0.32] - 2026-05-31

### 修复
- 修复设置界面下拉框在深色模式下全白不可见问题：添加select元素自定义样式，移除默认外观并设置深色背景

## [1.0.31] - 2026-05-31

### 修复
- 修复最近任务按钮图标文字布局：添加flex-shrink:0、white-space:nowrap、line-height:1，确保图标和文字水平排列

## [1.0.30] - 2026-05-31

### 修复
- 修复最近任务按钮图标和文字垂直堆叠问题：添加inline-flex布局

## [1.0.29] - 2026-05-31

### 改进
- AboutDialog改造：ExternalLink图标
- ConfirmDialog改造：AlertTriangle/HelpCircle图标替换emoji

## [1.0.28] - 2026-05-31

### 改进
- 任务面板改造：ActiveTasksPanel/RecentTasksPanel添加Lucide图标
- StatusBar改造：Wifi/Radio图标替换状态圆点

## [1.0.27] - 2026-05-31

### 改进
- StepPanel改造：Lucide图标替换所有emoji，Loader2旋转动画
- 步骤节点、连接线保持原有动画逻辑

## [1.0.26] - 2026-05-31

### 改进
- UrlInput改造：Link/Play图标、聚焦发光、motion入场动画
- FileDropArea改造：Upload/FileAudio/FolderOpen图标、拖拽高亮动效、AnimatePresence错误提示

## [1.0.25] - 2026-05-31

### 改进
- Header顶栏改造：glassmorphism效果、Lucide图标、搜索聚焦发光
- ControlBar改造：Lucide图标 + AnimatePresence动画

## [1.0.24] - 2026-05-31

### 改进
- WorkspaceSidebar组件改造：使用Lucide图标和motion动画

## [1.0.23] - 2026-05-31

### 改进
- 开始UI全面改造：色彩体系+全局样式重构
- 添加shadcn/ui组件库支持（button, card, input, badge, dialog）
- 更新Tailwind CSS配置以支持shadcn/ui设计系统
- 添加glassmorphism效果、紫色渐变主题、微交互动画

## [1.0.22] - 2026-05-31

### 修复
- 修复本地文件任务历史记录显示完整文件路径的问题：为本地文件设置文件名作为初始标题
- 修复标题清理未应用到URL回退字段的bug
- 改进cleanTitle：URL链接不做路径分割，文件路径额外清理_music/_audio等后缀

## [1.0.21] - 2026-05-31

### 优化
- 改进标题清理功能：更好地处理长文件路径和长文件名
- 新增日期前缀清理：移除2026-05-18_等日期前缀
- 标题长度限制：超过50个字符自动截断并添加省略号

## [1.0.20] - 2026-05-31

### 修复
- 修复便携版启动时缺少ffmpeg.dll的错误
- 将electron的ffmpeg.dll打包到应用目录

## [1.0.19] - 2026-05-31

### 优化
- 历史记录标题清理增强：支持处理本地文件路径，自动提取文件名作为标题
- 移除文件路径前缀（如G:\Podcast\）和文件扩展名（如.mp3）

## [1.0.18] - 2026-05-31

### 优化
- 历史记录标题清理增强：新增支持移除Vol5、Vol.5、Volume、V5等卷号标识

## [1.0.17] - 2026-05-31

### 优化
- 历史记录标题显示优化：只显示核心标题，自动移除期数（第X期、EP01）和日期（2024-01-01）等前缀

## [1.0.16] - 2026-05-31

### 优化
- 笔记分类调整为4个固定类别，与现有文件夹结构匹配：
  - 科技商业（AI/编程/互联网/创业/投资/管理/经济/技术）
  - 每日资讯（新闻/时事/热点/行业动态）
  - 社会心理（社会/心理/人际关系/职场/教育/哲学）
  - 生活文化（生活/健康/旅行/美食/历史/艺术/文学）

## [1.0.15] - 2026-05-31

### 优化
- 增加AI输出token限制：从4096提升到8192，支持更长的笔记内容
- 提示词优化：明确要求AI覆盖播客从头到尾的所有重要内容，避免遗漏后半部分

## [1.0.14] - 2026-05-31

### 优化
- 简化笔记分类为6个固定类别：科技、商业、文化、职场、生活、科学
- AI现在从固定分类列表中选择，而不是自由生成分类
- 减少文件夹数量，使目录结构更清晰

## [1.0.13] - 2026-05-31

### 新增
- 笔记自动分类：根据AI生成的category字段自动创建对应文件夹并存储笔记
- 支持分类示例：科技、商业、文化、历史、职场、生活、科学、心理、教育、旅行、美食、交通等

### 优化
- 笔记文件路径现在显示相对路径，更清晰地展示分类结构
- 未检测到分类信息时，笔记将保存到根目录并给出提示

## [1.0.12] - 2026-05-31

### 修复
- 修复设置对话框关闭确认使用原生window.confirm的问题，改为自定义ConfirmDialog组件
- 保持UI一致性：设置关闭确认与删除确认使用相同的对话框样式

## [1.0.11] - 2026-05-31

### 新增
- 动态加载模型列表：填写API Key后可从供应商API获取可用模型列表
- 添加"加载模型"按钮，支持实时获取供应商支持的模型

### 变更
- 优化模型选择体验：优先显示从API获取的模型列表
- DeepSeek模型默认使用V4系列

## [1.0.10] - 2026-05-31

### 变更
- 更新DeepSeek模型列表：新增V4 Flash和V4 Pro主力模型
- 旧版deepseek-chat和deepseek-reasoner标记为即将弃用
- 更新API地址为 https://api.deepseek.com

## [1.0.9] - 2026-05-31

### 新增
- 多AI供应商支持：新增7个预设供应商（DeepSeek、OpenAI、Moonshot、智谱AI、通义千问、零一万物、MiniMax）
- 供应商选择界面：可视化供应商卡片选择，显示配置状态
- 供应商详情配置：API Key、API地址、模型选择，支持官网链接和密钥获取链接
- 自定义供应商：支持添加自定义OpenAI兼容接口
- 供应商配置持久化：每个供应商独立保存配置

### 变更
- 重构AI调用模块：从单一DeepSeek调用改为通用AI客户端
- 更新配置结构：新增ai_provider和ai_providers字段
- 优化设置界面：接口与通知标签页重新设计

## [1.0.8] - 2026-05-31

### 变更
- 设置对话框重新设计：从单页滚动布局改为左侧边栏导航 + 右侧内容面板布局
- 新增4个标签页：接口与通知、转写偏好、语音模型、工具维护
- 优化对话框尺寸：720×520px，提升用户体验

## [1.0.7] - 2026-05-31

### 变更
- 移除设置对话框中的"关于"部分，改为独立的关于对话框

## [1.0.6] - 2026-05-31

### 新增
- 设置对话框全面改进：输入验证、脏状态检测、通知开关、清理临时文件、关于对话框
- 关于对话框：展示版本号、功能流程、技术栈标签、项目链接、许可证信息
- 通知开关：用户可控制是否接收系统通知
- 高级设置折叠：Whisper exe 路径移入可折叠的高级设置区域
- 清理临时文件功能：在设置中可一键清理缓存

### 变更
- 移除设置对话框中的内部开发注释，改为用户友好的描述
- 保存按钮在无更改时变灰，保存成功后显示绿色反馈
- DeepSeek API Key 标记为必填字段，空值时显示错误提示
- 关闭设置对话框时如有未保存更改会弹出确认提示

## [1.0.5] - 2026-05-31

### 新增
- 添加搜索功能：支持搜索 Obsidian 笔记文件（Ctrl+K 快捷键）
- 添加系统通知功能：播客处理完成/失败时发送 Windows 通知
- 添加版本号显示：Header 栏显示当前版本号
- 添加删除任务二次确认：防止误删任务记录
- 添加 DeepSeek API 自动重试机制：网络失败时自动重试

### 修复
- 修复通知功能漏洞：添加点击通知恢复窗口、设置 AppUserModelID
- 修复搜索栏无功能问题：实现完整的搜索功能
- 修复 DeepSeek API 调用失败无重试机制的问题
- 修复删除任务无确认对话框的问题
- 修复 UI 界面没有显示版本号的问题

### 变更
- 清理调试代码：移除所有调试日志代码
- 优化代码结构：改进错误处理和用户体验
- 添加版本号管理脚本和文档

## [3.0.0] - 2026-05-27

### 新增
- 项目标准化整理：添加 ESLint + Prettier 代码规范配置
- 统一项目目录结构，梳理依赖分类（dependencies / devDependencies）
- 补全标准文档体系（CHANGELOG.md、CONTRIBUTING.md、PROJECT_STRUCTURE.md）
- 添加 `npm run lint` / `npm run format` 等代码质量脚本
- 补充 `package.json` 元信息字段（author、license、repository、keywords 等）

### 变更
- 移动根目录零散资源文件（`播客笔记_256.png`、`yangtu.png`）到 `build/` 目录
- 清理根目录冗余文件（`index.js`、`dist-debug-L.txt`）
- 删除三份重复的构建输出目录（`dist-personal/`、`dist-personal-v2/`、`dist-release/`）
- 修复 `.gitignore`：添加 dist-* 目录忽略规则，移除全局图片屏蔽规则

### 修复
- 修复 `framer-motion`、`react`、`react-dom` 被错误归类为 devDependencies 的问题

## [2.0.0] - 2026-05-26

### 新增
- 实体卡片自动生成功能（人物、项目、概念）
- 大分类自动归档引擎
- Obsidian 双向链接支持
- 飞书消息轮询集成
- Whisper 模型管理面板
- 任务恢复机制

## [1.0.0] - 2026-05-24

### 新增
- 初始版本发布
- 小宇宙播客链接解析与音频下载
- Whisper 语音转文字
- DeepSeek AI 笔记生成
- Electron 桌面应用框架
- React 用户界面
